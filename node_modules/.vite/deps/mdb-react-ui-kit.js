import {
  createPopper,
  require_react_fast_compare
} from "./chunk-VFM3DZNY.js";
import {
  require_warning
} from "./chunk-JMQKMFWN.js";
import "./chunk-HIYV2TPJ.js";
import {
  require_jsx_runtime
} from "./chunk-UFHFCCDD.js";
import {
  require_react_dom
} from "./chunk-QJV3R4PZ.js";
import {
  require_react
} from "./chunk-JFTBQ7A7.js";
import {
  __toESM
} from "./chunk-AC2VUBZ6.js";

// node_modules/mdb-react-ui-kit/dist/mdb-react-ui-kit.esm.js
var import_jsx_runtime = __toESM(require_jsx_runtime());
var import_react = __toESM(require_react());

// node_modules/clsx/dist/clsx.m.js
function toVal(mix) {
  var k, y, str = "";
  if (typeof mix === "string" || typeof mix === "number") {
    str += mix;
  } else if (typeof mix === "object") {
    if (Array.isArray(mix)) {
      for (k = 0; k < mix.length; k++) {
        if (mix[k]) {
          if (y = toVal(mix[k])) {
            str && (str += " ");
            str += y;
          }
        }
      }
    } else {
      for (k in mix) {
        if (mix[k]) {
          str && (str += " ");
          str += k;
        }
      }
    }
  }
  return str;
}
function clsx_m_default() {
  var i2 = 0, tmp, x2, str = "";
  while (i2 < arguments.length) {
    if (tmp = arguments[i2++]) {
      if (x2 = toVal(tmp)) {
        str && (str += " ");
        str += x2;
      }
    }
  }
  return str;
}

// node_modules/mdb-react-ui-kit/dist/mdb-react-ui-kit.esm.js
var import_react_dom = __toESM(require_react_dom());

// node_modules/react-popper/lib/esm/Popper.js
var React4 = __toESM(require_react());

// node_modules/react-popper/lib/esm/Manager.js
var React = __toESM(require_react());
var ManagerReferenceNodeContext = React.createContext();
var ManagerReferenceNodeSetterContext = React.createContext();

// node_modules/react-popper/lib/esm/utils.js
var React2 = __toESM(require_react());
var fromEntries = function fromEntries2(entries) {
  return entries.reduce(function(acc, _ref) {
    var key = _ref[0], value = _ref[1];
    acc[key] = value;
    return acc;
  }, {});
};
var useIsomorphicLayoutEffect = typeof window !== "undefined" && window.document && window.document.createElement ? React2.useLayoutEffect : React2.useEffect;

// node_modules/react-popper/lib/esm/usePopper.js
var React3 = __toESM(require_react());
var ReactDOM = __toESM(require_react_dom());
var import_react_fast_compare = __toESM(require_react_fast_compare());
var EMPTY_MODIFIERS = [];
var usePopper = function usePopper2(referenceElement, popperElement, options) {
  if (options === void 0) {
    options = {};
  }
  var prevOptions = React3.useRef(null);
  var optionsWithDefaults = {
    onFirstUpdate: options.onFirstUpdate,
    placement: options.placement || "bottom",
    strategy: options.strategy || "absolute",
    modifiers: options.modifiers || EMPTY_MODIFIERS
  };
  var _React$useState = React3.useState({
    styles: {
      popper: {
        position: optionsWithDefaults.strategy,
        left: "0",
        top: "0"
      },
      arrow: {
        position: "absolute"
      }
    },
    attributes: {}
  }), state = _React$useState[0], setState = _React$useState[1];
  var updateStateModifier = React3.useMemo(function() {
    return {
      name: "updateState",
      enabled: true,
      phase: "write",
      fn: function fn2(_ref) {
        var state2 = _ref.state;
        var elements = Object.keys(state2.elements);
        ReactDOM.flushSync(function() {
          setState({
            styles: fromEntries(elements.map(function(element) {
              return [element, state2.styles[element] || {}];
            })),
            attributes: fromEntries(elements.map(function(element) {
              return [element, state2.attributes[element]];
            }))
          });
        });
      },
      requires: ["computeStyles"]
    };
  }, []);
  var popperOptions = React3.useMemo(function() {
    var newOptions = {
      onFirstUpdate: optionsWithDefaults.onFirstUpdate,
      placement: optionsWithDefaults.placement,
      strategy: optionsWithDefaults.strategy,
      modifiers: [].concat(optionsWithDefaults.modifiers, [updateStateModifier, {
        name: "applyStyles",
        enabled: false
      }])
    };
    if ((0, import_react_fast_compare.default)(prevOptions.current, newOptions)) {
      return prevOptions.current || newOptions;
    } else {
      prevOptions.current = newOptions;
      return newOptions;
    }
  }, [optionsWithDefaults.onFirstUpdate, optionsWithDefaults.placement, optionsWithDefaults.strategy, optionsWithDefaults.modifiers, updateStateModifier]);
  var popperInstanceRef = React3.useRef();
  useIsomorphicLayoutEffect(function() {
    if (popperInstanceRef.current) {
      popperInstanceRef.current.setOptions(popperOptions);
    }
  }, [popperOptions]);
  useIsomorphicLayoutEffect(function() {
    if (referenceElement == null || popperElement == null) {
      return;
    }
    var createPopper5 = options.createPopper || createPopper;
    var popperInstance = createPopper5(referenceElement, popperElement, popperOptions);
    popperInstanceRef.current = popperInstance;
    return function() {
      popperInstance.destroy();
      popperInstanceRef.current = null;
    };
  }, [referenceElement, popperElement, options.createPopper]);
  return {
    state: popperInstanceRef.current ? popperInstanceRef.current.state : null,
    styles: state.styles,
    attributes: state.attributes,
    update: popperInstanceRef.current ? popperInstanceRef.current.update : null,
    forceUpdate: popperInstanceRef.current ? popperInstanceRef.current.forceUpdate : null
  };
};

// node_modules/react-popper/lib/esm/Reference.js
var React5 = __toESM(require_react());
var import_warning = __toESM(require_warning());

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/enums.js
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var clippingParents = "clippingParents";
var viewport = "viewport";
var popper = "popper";
var reference = "reference";
var variationPlacements = basePlacements.reduce(function(acc, placement) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
var placements = [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getNodeName.js
function getNodeName(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getWindow.js
function getWindow(node) {
  if (node == null) {
    return window;
  }
  if (node.toString() !== "[object Window]") {
    var ownerDocument = node.ownerDocument;
    return ownerDocument ? ownerDocument.defaultView || window : window;
  }
  return node;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/instanceOf.js
function isElement(node) {
  var OwnElement = getWindow(node).Element;
  return node instanceof OwnElement || node instanceof Element;
}
function isHTMLElement(node) {
  var OwnElement = getWindow(node).HTMLElement;
  return node instanceof OwnElement || node instanceof HTMLElement;
}
function isShadowRoot(node) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow(node).ShadowRoot;
  return node instanceof OwnElement || node instanceof ShadowRoot;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/applyStyles.js
function applyStyles(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function(name) {
    var style = state.styles[name] || {};
    var attributes = state.attributes[name] || {};
    var element = state.elements[name];
    if (!isHTMLElement(element) || !getNodeName(element)) {
      return;
    }
    Object.assign(element.style, style);
    Object.keys(attributes).forEach(function(name2) {
      var value = attributes[name2];
      if (value === false) {
        element.removeAttribute(name2);
      } else {
        element.setAttribute(name2, value === true ? "" : value);
      }
    });
  });
}
function effect(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: "0",
      top: "0",
      margin: "0"
    },
    arrow: {
      position: "absolute"
    },
    reference: {}
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;
  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }
  return function() {
    Object.keys(state.elements).forEach(function(name) {
      var element = state.elements[name];
      var attributes = state.attributes[name] || {};
      var styleProperties = Object.keys(state.styles.hasOwnProperty(name) ? state.styles[name] : initialStyles[name]);
      var style = styleProperties.reduce(function(style2, property) {
        style2[property] = "";
        return style2;
      }, {});
      if (!isHTMLElement(element) || !getNodeName(element)) {
        return;
      }
      Object.assign(element.style, style);
      Object.keys(attributes).forEach(function(attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
}
var applyStyles_default = {
  name: "applyStyles",
  enabled: true,
  phase: "write",
  fn: applyStyles,
  effect,
  requires: ["computeStyles"]
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/getBasePlacement.js
function getBasePlacement(placement) {
  return placement.split("-")[0];
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/math.js
var max = Math.max;
var min = Math.min;
var round = Math.round;

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getBoundingClientRect.js
function getBoundingClientRect(element, includeScale) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  var rect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;
  if (isHTMLElement(element) && includeScale) {
    var offsetHeight = element.offsetHeight;
    var offsetWidth = element.offsetWidth;
    if (offsetWidth > 0) {
      scaleX = round(rect.width) / offsetWidth || 1;
    }
    if (offsetHeight > 0) {
      scaleY = round(rect.height) / offsetHeight || 1;
    }
  }
  return {
    width: rect.width / scaleX,
    height: rect.height / scaleY,
    top: rect.top / scaleY,
    right: rect.right / scaleX,
    bottom: rect.bottom / scaleY,
    left: rect.left / scaleX,
    x: rect.left / scaleX,
    y: rect.top / scaleY
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getLayoutRect.js
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect(element);
  var width = element.offsetWidth;
  var height = element.offsetHeight;
  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }
  if (Math.abs(clientRect.height - height) <= 1) {
    height = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width,
    height
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/contains.js
function contains(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot(rootNode)) {
    var next = child;
    do {
      if (next && parent.isSameNode(next)) {
        return true;
      }
      next = next.parentNode || next.host;
    } while (next);
  }
  return false;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getComputedStyle.js
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/isTableElement.js
function isTableElement(element) {
  return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getDocumentElement.js
function getDocumentElement(element) {
  return ((isElement(element) ? element.ownerDocument : (
    // $FlowFixMe[prop-missing]
    element.document
  )) || window.document).documentElement;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getParentNode.js
function getParentNode(element) {
  if (getNodeName(element) === "html") {
    return element;
  }
  return (
    // this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    element.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    element.parentNode || // DOM Element detected
    (isShadowRoot(element) ? element.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    getDocumentElement(element)
  );
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getOffsetParent.js
function getTrueOffsetParent(element) {
  if (!isHTMLElement(element) || // https://github.com/popperjs/popper-core/issues/837
  getComputedStyle2(element).position === "fixed") {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock(element) {
  var isFirefox = navigator.userAgent.toLowerCase().indexOf("firefox") !== -1;
  var isIE = navigator.userAgent.indexOf("Trident") !== -1;
  if (isIE && isHTMLElement(element)) {
    var elementCss = getComputedStyle2(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode(element);
  if (isShadowRoot(currentNode)) {
    currentNode = currentNode.host;
  }
  while (isHTMLElement(currentNode) && ["html", "body"].indexOf(getNodeName(currentNode)) < 0) {
    var css = getComputedStyle2(currentNode);
    if (css.transform !== "none" || css.perspective !== "none" || css.contain === "paint" || ["transform", "perspective"].indexOf(css.willChange) !== -1 || isFirefox && css.willChange === "filter" || isFirefox && css.filter && css.filter !== "none") {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent(element) {
  var window2 = getWindow(element);
  var offsetParent = getTrueOffsetParent(element);
  while (offsetParent && isTableElement(offsetParent) && getComputedStyle2(offsetParent).position === "static") {
    offsetParent = getTrueOffsetParent(offsetParent);
  }
  if (offsetParent && (getNodeName(offsetParent) === "html" || getNodeName(offsetParent) === "body" && getComputedStyle2(offsetParent).position === "static")) {
    return window2;
  }
  return offsetParent || getContainingBlock(element) || window2;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/getMainAxisFromPlacement.js
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/within.js
function within(min2, value, max2) {
  return max(min2, min(value, max2));
}
function withinMaxClamp(min2, value, max2) {
  var v = within(min2, value, max2);
  return v > max2 ? max2 : v;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/getFreshSideObject.js
function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/mergePaddingObject.js
function mergePaddingObject(paddingObject) {
  return Object.assign({}, getFreshSideObject(), paddingObject);
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/expandToHashMap.js
function expandToHashMap(value, keys) {
  return keys.reduce(function(hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/arrow.js
var toPaddingObject = function toPaddingObject2(padding, state) {
  padding = typeof padding === "function" ? padding(Object.assign({}, state.rects, {
    placement: state.placement
  })) : padding;
  return mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
};
function arrow(_ref) {
  var _state$modifiersData$;
  var state = _ref.state, name = _ref.name, options = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var basePlacement = getBasePlacement(state.placement);
  var axis = getMainAxisFromPlacement(basePlacement);
  var isVertical = [left, right].indexOf(basePlacement) >= 0;
  var len = isVertical ? "height" : "width";
  if (!arrowElement || !popperOffsets2) {
    return;
  }
  var paddingObject = toPaddingObject(options.padding, state);
  var arrowRect = getLayoutRect(arrowElement);
  var minProp = axis === "y" ? top : left;
  var maxProp = axis === "y" ? bottom : right;
  var endDiff = state.rects.reference[len] + state.rects.reference[axis] - popperOffsets2[axis] - state.rects.popper[len];
  var startDiff = popperOffsets2[axis] - state.rects.reference[axis];
  var arrowOffsetParent = getOffsetParent(arrowElement);
  var clientSize = arrowOffsetParent ? axis === "y" ? arrowOffsetParent.clientHeight || 0 : arrowOffsetParent.clientWidth || 0 : 0;
  var centerToReference = endDiff / 2 - startDiff / 2;
  var min2 = paddingObject[minProp];
  var max2 = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset2 = within(min2, center, max2);
  var axisProp = axis;
  state.modifiersData[name] = (_state$modifiersData$ = {}, _state$modifiersData$[axisProp] = offset2, _state$modifiersData$.centerOffset = offset2 - center, _state$modifiersData$);
}
function effect2(_ref2) {
  var state = _ref2.state, options = _ref2.options;
  var _options$element = options.element, arrowElement = _options$element === void 0 ? "[data-popper-arrow]" : _options$element;
  if (arrowElement == null) {
    return;
  }
  if (typeof arrowElement === "string") {
    arrowElement = state.elements.popper.querySelector(arrowElement);
    if (!arrowElement) {
      return;
    }
  }
  if (true) {
    if (!isHTMLElement(arrowElement)) {
      console.error(['Popper: "arrow" element must be an HTMLElement (not an SVGElement).', "To use an SVG arrow, wrap it in an HTMLElement that will be used as", "the arrow."].join(" "));
    }
  }
  if (!contains(state.elements.popper, arrowElement)) {
    if (true) {
      console.error(['Popper: "arrow" modifier\'s `element` must be a child of the popper', "element."].join(" "));
    }
    return;
  }
  state.elements.arrow = arrowElement;
}
var arrow_default = {
  name: "arrow",
  enabled: true,
  phase: "main",
  fn: arrow,
  effect: effect2,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"]
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/getVariation.js
function getVariation(placement) {
  return placement.split("-")[1];
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/computeStyles.js
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function roundOffsetsByDPR(_ref) {
  var x2 = _ref.x, y = _ref.y;
  var win = window;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round(x2 * dpr) / dpr || 0,
    y: round(y * dpr) / dpr || 0
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper2 = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, variation = _ref2.variation, offsets = _ref2.offsets, position = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets, isFixed = _ref2.isFixed;
  var _offsets$x = offsets.x, x2 = _offsets$x === void 0 ? 0 : _offsets$x, _offsets$y = offsets.y, y = _offsets$y === void 0 ? 0 : _offsets$y;
  var _ref3 = typeof roundOffsets === "function" ? roundOffsets({
    x: x2,
    y
  }) : {
    x: x2,
    y
  };
  x2 = _ref3.x;
  y = _ref3.y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent = getOffsetParent(popper2);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent === getWindow(popper2)) {
      offsetParent = getDocumentElement(popper2);
      if (getComputedStyle2(offsetParent).position !== "static" && position === "absolute") {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent = offsetParent;
    if (placement === top || (placement === left || placement === right) && variation === end) {
      sideY = bottom;
      var offsetY = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.height : (
        // $FlowFixMe[prop-missing]
        offsetParent[heightProp]
      );
      y -= offsetY - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }
    if (placement === left || (placement === top || placement === bottom) && variation === end) {
      sideX = right;
      var offsetX = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.width : (
        // $FlowFixMe[prop-missing]
        offsetParent[widthProp]
      );
      x2 -= offsetX - popperRect.width;
      x2 *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign({
    position
  }, adaptive && unsetSides);
  var _ref4 = roundOffsets === true ? roundOffsetsByDPR({
    x: x2,
    y
  }) : {
    x: x2,
    y
  };
  x2 = _ref4.x;
  y = _ref4.y;
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) <= 1 ? "translate(" + x2 + "px, " + y + "px)" : "translate3d(" + x2 + "px, " + y + "px, 0)", _Object$assign));
  }
  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : "", _Object$assign2[sideX] = hasX ? x2 + "px" : "", _Object$assign2.transform = "", _Object$assign2));
}
function computeStyles(_ref5) {
  var state = _ref5.state, options = _ref5.options;
  var _options$gpuAccelerat = options.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  if (true) {
    var transitionProperty = getComputedStyle2(state.elements.popper).transitionProperty || "";
    if (adaptive && ["transform", "top", "right", "bottom", "left"].some(function(property) {
      return transitionProperty.indexOf(property) >= 0;
    })) {
      console.warn(["Popper: Detected CSS transitions on at least one of the following", 'CSS properties: "transform", "top", "right", "bottom", "left".', "\n\n", 'Disable the "computeStyles" modifier\'s `adaptive` option to allow', "for smooth transitions, or remove these properties from the CSS", "transition declaration on the popper element if only transitioning", "opacity or background-color for example.", "\n\n", "We recommend using the popper element as a wrapper around an inner", "element that can have any CSS property transitioned for animations."].join(" "));
    }
  }
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    variation: getVariation(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration,
    isFixed: state.options.strategy === "fixed"
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive,
      roundOffsets
    })));
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: "absolute",
      adaptive: false,
      roundOffsets
    })));
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement
  });
}
var computeStyles_default = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {}
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/eventListeners.js
var passive = {
  passive: true
};
function effect3(_ref) {
  var state = _ref.state, instance = _ref.instance, options = _ref.options;
  var _options$scroll = options.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options.resize, resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
  if (scroll) {
    scrollParents.forEach(function(scrollParent) {
      scrollParent.addEventListener("scroll", instance.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance.update, passive);
  }
  return function() {
    if (scroll) {
      scrollParents.forEach(function(scrollParent) {
        scrollParent.removeEventListener("scroll", instance.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance.update, passive);
    }
  };
}
var eventListeners_default = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {
  },
  effect: effect3,
  data: {}
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/getOppositePlacement.js
var hash = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, function(matched) {
    return hash[matched];
  });
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/getOppositeVariationPlacement.js
var hash2 = {
  start: "end",
  end: "start"
};
function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function(matched) {
    return hash2[matched];
  });
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getWindowScroll.js
function getWindowScroll(node) {
  var win = getWindow(node);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft,
    scrollTop
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getWindowScrollBarX.js
function getWindowScrollBarX(element) {
  return getBoundingClientRect(getDocumentElement(element)).left + getWindowScroll(element).scrollLeft;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getViewportRect.js
function getViewportRect(element) {
  var win = getWindow(element);
  var html = getDocumentElement(element);
  var visualViewport = win.visualViewport;
  var width = html.clientWidth;
  var height = html.clientHeight;
  var x2 = 0;
  var y = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    if (!/^((?!chrome|android).)*safari/i.test(navigator.userAgent)) {
      x2 = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height,
    x: x2 + getWindowScrollBarX(element),
    y
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getDocumentRect.js
function getDocumentRect(element) {
  var _element$ownerDocumen;
  var html = getDocumentElement(element);
  var winScroll = getWindowScroll(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = max(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = max(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x2 = -winScroll.scrollLeft + getWindowScrollBarX(element);
  var y = -winScroll.scrollTop;
  if (getComputedStyle2(body || html).direction === "rtl") {
    x2 += max(html.clientWidth, body ? body.clientWidth : 0) - width;
  }
  return {
    width,
    height,
    x: x2,
    y
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/isScrollParent.js
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle2(element), overflow = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getScrollParent.js
function getScrollParent(node) {
  if (["html", "body", "#document"].indexOf(getNodeName(node)) >= 0) {
    return node.ownerDocument.body;
  }
  if (isHTMLElement(node) && isScrollParent(node)) {
    return node;
  }
  return getScrollParent(getParentNode(node));
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/listScrollParents.js
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent = getScrollParent(element);
  var isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = getWindow(scrollParent);
  var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent) ? scrollParent : []) : scrollParent;
  var updatedList = list.concat(target);
  return isBody ? updatedList : (
    // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
    updatedList.concat(listScrollParents(getParentNode(target)))
  );
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/rectToClientRect.js
function rectToClientRect(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  });
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getClippingRect.js
function getInnerBoundingClientRect(element) {
  var rect = getBoundingClientRect(element);
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}
function getClientRectFromMixedType(element, clippingParent) {
  return clippingParent === viewport ? rectToClientRect(getViewportRect(element)) : isElement(clippingParent) ? getInnerBoundingClientRect(clippingParent) : rectToClientRect(getDocumentRect(getDocumentElement(element)));
}
function getClippingParents(element) {
  var clippingParents2 = listScrollParents(getParentNode(element));
  var canEscapeClipping = ["absolute", "fixed"].indexOf(getComputedStyle2(element).position) >= 0;
  var clipperElement = canEscapeClipping && isHTMLElement(element) ? getOffsetParent(element) : element;
  if (!isElement(clipperElement)) {
    return [];
  }
  return clippingParents2.filter(function(clippingParent) {
    return isElement(clippingParent) && contains(clippingParent, clipperElement) && getNodeName(clippingParent) !== "body";
  });
}
function getClippingRect(element, boundary, rootBoundary) {
  var mainClippingParents = boundary === "clippingParents" ? getClippingParents(element) : [].concat(boundary);
  var clippingParents2 = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents2[0];
  var clippingRect = clippingParents2.reduce(function(accRect, clippingParent) {
    var rect = getClientRectFromMixedType(element, clippingParent);
    accRect.top = max(rect.top, accRect.top);
    accRect.right = min(rect.right, accRect.right);
    accRect.bottom = min(rect.bottom, accRect.bottom);
    accRect.left = max(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromMixedType(element, firstClippingParent));
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/computeOffsets.js
function computeOffsets(_ref) {
  var reference2 = _ref.reference, element = _ref.element, placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference2.x + reference2.width / 2 - element.width / 2;
  var commonY = reference2.y + reference2.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference2.y - element.height
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference2.y + reference2.height
      };
      break;
    case right:
      offsets = {
        x: reference2.x + reference2.width,
        y: commonY
      };
      break;
    case left:
      offsets = {
        x: reference2.x - element.width,
        y: commonY
      };
      break;
    default:
      offsets = {
        x: reference2.x,
        y: reference2.y
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] = offsets[mainAxis] - (reference2[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] = offsets[mainAxis] + (reference2[len] / 2 - element[len] / 2);
        break;
      default:
    }
  }
  return offsets;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/detectOverflow.js
function detectOverflow(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, _options$placement = _options.placement, placement = _options$placement === void 0 ? state.placement : _options$placement, _options$boundary = _options.boundary, boundary = _options$boundary === void 0 ? clippingParents : _options$boundary, _options$rootBoundary = _options.rootBoundary, rootBoundary = _options$rootBoundary === void 0 ? viewport : _options$rootBoundary, _options$elementConte = _options.elementContext, elementContext = _options$elementConte === void 0 ? popper : _options$elementConte, _options$altBoundary = _options.altBoundary, altBoundary = _options$altBoundary === void 0 ? false : _options$altBoundary, _options$padding = _options.padding, padding = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
  var altContext = elementContext === popper ? reference : popper;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = getClippingRect(isElement(element) ? element : element.contextElement || getDocumentElement(state.elements.popper), boundary, rootBoundary);
  var referenceClientRect = getBoundingClientRect(state.elements.reference);
  var popperOffsets2 = computeOffsets({
    reference: referenceClientRect,
    element: popperRect,
    strategy: "absolute",
    placement
  });
  var popperClientRect = rectToClientRect(Object.assign({}, popperRect, popperOffsets2));
  var elementClientRect = elementContext === popper ? popperClientRect : referenceClientRect;
  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom: elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right: elementClientRect.right - clippingClientRect.right + paddingObject.right
  };
  var offsetData = state.modifiersData.offset;
  if (elementContext === popper && offsetData) {
    var offset2 = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function(key) {
      var multiply = [right, bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [top, bottom].indexOf(key) >= 0 ? "y" : "x";
      overflowOffsets[key] += offset2[axis] * multiply;
    });
  }
  return overflowOffsets;
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/computeAutoPlacement.js
function computeAutoPlacement(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, placement = _options.placement, boundary = _options.boundary, rootBoundary = _options.rootBoundary, padding = _options.padding, flipVariations = _options.flipVariations, _options$allowedAutoP = _options.allowedAutoPlacements, allowedAutoPlacements = _options$allowedAutoP === void 0 ? placements : _options$allowedAutoP;
  var variation = getVariation(placement);
  var placements2 = variation ? flipVariations ? variationPlacements : variationPlacements.filter(function(placement2) {
    return getVariation(placement2) === variation;
  }) : basePlacements;
  var allowedPlacements = placements2.filter(function(placement2) {
    return allowedAutoPlacements.indexOf(placement2) >= 0;
  });
  if (allowedPlacements.length === 0) {
    allowedPlacements = placements2;
    if (true) {
      console.error(["Popper: The `allowedAutoPlacements` option did not allow any", "placements. Ensure the `placement` option matches the variation", "of the allowed placements.", 'For example, "auto" cannot be used to allow "bottom-start".', 'Use "auto-start" instead.'].join(" "));
    }
  }
  var overflows = allowedPlacements.reduce(function(acc, placement2) {
    acc[placement2] = detectOverflow(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding
    })[getBasePlacement(placement2)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function(a, b) {
    return overflows[a] - overflows[b];
  });
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/flip.js
function getExpandedFallbackPlacements(placement) {
  if (getBasePlacement(placement) === auto) {
    return [];
  }
  var oppositePlacement = getOppositePlacement(placement);
  return [getOppositeVariationPlacement(placement), oppositePlacement, getOppositeVariationPlacement(oppositePlacement)];
}
function flip(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  if (state.modifiersData[name]._skip) {
    return;
  }
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis, specifiedFallbackPlacements = options.fallbackPlacements, padding = options.padding, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, _options$flipVariatio = options.flipVariations, flipVariations = _options$flipVariatio === void 0 ? true : _options$flipVariatio, allowedAutoPlacements = options.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = getBasePlacement(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipVariations ? [getOppositePlacement(preferredPlacement)] : getExpandedFallbackPlacements(preferredPlacement));
  var placements2 = [preferredPlacement].concat(fallbackPlacements).reduce(function(acc, placement2) {
    return acc.concat(getBasePlacement(placement2) === auto ? computeAutoPlacement(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding,
      flipVariations,
      allowedAutoPlacements
    }) : placement2);
  }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = /* @__PURE__ */ new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements2[0];
  for (var i2 = 0; i2 < placements2.length; i2++) {
    var placement = placements2[i2];
    var _basePlacement = getBasePlacement(placement);
    var isStartVariation = getVariation(placement) === start;
    var isVertical = [top, bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? "width" : "height";
    var overflow = detectOverflow(state, {
      placement,
      boundary,
      rootBoundary,
      altBoundary,
      padding
    });
    var mainVariationSide = isVertical ? isStartVariation ? right : left : isStartVariation ? bottom : top;
    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = getOppositePlacement(mainVariationSide);
    }
    var altVariationSide = getOppositePlacement(mainVariationSide);
    var checks = [];
    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }
    if (checkAltAxis) {
      checks.push(overflow[mainVariationSide] <= 0, overflow[altVariationSide] <= 0);
    }
    if (checks.every(function(check) {
      return check;
    })) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }
    checksMap.set(placement, checks);
  }
  if (makeFallbackChecks) {
    var numberOfChecks = flipVariations ? 3 : 1;
    var _loop = function _loop2(_i2) {
      var fittingPlacement = placements2.find(function(placement2) {
        var checks2 = checksMap.get(placement2);
        if (checks2) {
          return checks2.slice(0, _i2).every(function(check) {
            return check;
          });
        }
      });
      if (fittingPlacement) {
        firstFittingPlacement = fittingPlacement;
        return "break";
      }
    };
    for (var _i = numberOfChecks; _i > 0; _i--) {
      var _ret = _loop(_i);
      if (_ret === "break")
        break;
    }
  }
  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
}
var flip_default = {
  name: "flip",
  enabled: true,
  phase: "main",
  fn: flip,
  requiresIfExists: ["offset"],
  data: {
    _skip: false
  }
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/hide.js
function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0
    };
  }
  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x
  };
}
function isAnySideFullyClipped(overflow) {
  return [top, right, bottom, left].some(function(side) {
    return overflow[side] >= 0;
  });
}
function hide(_ref) {
  var state = _ref.state, name = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = detectOverflow(state, {
    elementContext: "reference"
  });
  var popperAltOverflow = detectOverflow(state, {
    altBoundary: true
  });
  var referenceClippingOffsets = getSideOffsets(referenceOverflow, referenceRect);
  var popperEscapeOffsets = getSideOffsets(popperAltOverflow, popperRect, preventedOffsets);
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name] = {
    referenceClippingOffsets,
    popperEscapeOffsets,
    isReferenceHidden,
    hasPopperEscaped
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-reference-hidden": isReferenceHidden,
    "data-popper-escaped": hasPopperEscaped
  });
}
var hide_default = {
  name: "hide",
  enabled: true,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: hide
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/offset.js
function distanceAndSkiddingToXY(placement, rects, offset2) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref = typeof offset2 === "function" ? offset2(Object.assign({}, rects, {
    placement
  })) : offset2, skidding = _ref[0], distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}
function offset(_ref2) {
  var state = _ref2.state, options = _ref2.options, name = _ref2.name;
  var _options$offset = options.offset, offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements.reduce(function(acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement], x2 = _data$state$placement.x, y = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x2;
    state.modifiersData.popperOffsets.y += y;
  }
  state.modifiersData[name] = data;
}
var offset_default = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/popperOffsets.js
function popperOffsets(_ref) {
  var state = _ref.state, name = _ref.name;
  state.modifiersData[name] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: "absolute",
    placement: state.placement
  });
}
var popperOffsets_default = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {}
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/getAltAxis.js
function getAltAxis(axis) {
  return axis === "x" ? "y" : "x";
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/modifiers/preventOverflow.js
function preventOverflow(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, padding = options.padding, _options$tether = options.tether, tether = _options$tether === void 0 ? true : _options$tether, _options$tetherOffset = options.tetherOffset, tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = detectOverflow(state, {
    boundary,
    rootBoundary,
    padding,
    altBoundary
  });
  var basePlacement = getBasePlacement(state.placement);
  var variation = getVariation(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = getMainAxisFromPlacement(basePlacement);
  var altAxis = getAltAxis(mainAxis);
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue = typeof tetherOffset === "function" ? tetherOffset(Object.assign({}, state.rects, {
    placement: state.placement
  })) : tetherOffset;
  var normalizedTetherOffsetValue = typeof tetherOffsetValue === "number" ? {
    mainAxis: tetherOffsetValue,
    altAxis: tetherOffsetValue
  } : Object.assign({
    mainAxis: 0,
    altAxis: 0
  }, tetherOffsetValue);
  var offsetModifierState = state.modifiersData.offset ? state.modifiersData.offset[state.placement] : null;
  var data = {
    x: 0,
    y: 0
  };
  if (!popperOffsets2) {
    return;
  }
  if (checkMainAxis) {
    var _offsetModifierState$;
    var mainSide = mainAxis === "y" ? top : left;
    var altSide = mainAxis === "y" ? bottom : right;
    var len = mainAxis === "y" ? "height" : "width";
    var offset2 = popperOffsets2[mainAxis];
    var min2 = offset2 + overflow[mainSide];
    var max2 = offset2 - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === start ? -popperRect[len] : -referenceRect[len];
    var arrowElement = state.elements.arrow;
    var arrowRect = tether && arrowElement ? getLayoutRect(arrowElement) : {
      width: 0,
      height: 0
    };
    var arrowPaddingObject = state.modifiersData["arrow#persistent"] ? state.modifiersData["arrow#persistent"].padding : getFreshSideObject();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide];
    var arrowLen = within(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement ? referenceRect[len] / 2 - additive - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis : minLen - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis;
    var maxOffset = isBasePlacement ? -referenceRect[len] / 2 + additive + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis : maxLen + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis;
    var arrowOffsetParent = state.elements.arrow && getOffsetParent(state.elements.arrow);
    var clientOffset = arrowOffsetParent ? mainAxis === "y" ? arrowOffsetParent.clientTop || 0 : arrowOffsetParent.clientLeft || 0 : 0;
    var offsetModifierValue = (_offsetModifierState$ = offsetModifierState == null ? void 0 : offsetModifierState[mainAxis]) != null ? _offsetModifierState$ : 0;
    var tetherMin = offset2 + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = offset2 + maxOffset - offsetModifierValue;
    var preventedOffset = within(tether ? min(min2, tetherMin) : min2, offset2, tether ? max(max2, tetherMax) : max2);
    popperOffsets2[mainAxis] = preventedOffset;
    data[mainAxis] = preventedOffset - offset2;
  }
  if (checkAltAxis) {
    var _offsetModifierState$2;
    var _mainSide = mainAxis === "x" ? top : left;
    var _altSide = mainAxis === "x" ? bottom : right;
    var _offset = popperOffsets2[altAxis];
    var _len = altAxis === "y" ? "height" : "width";
    var _min = _offset + overflow[_mainSide];
    var _max = _offset - overflow[_altSide];
    var isOriginSide = [top, left].indexOf(basePlacement) !== -1;
    var _offsetModifierValue = (_offsetModifierState$2 = offsetModifierState == null ? void 0 : offsetModifierState[altAxis]) != null ? _offsetModifierState$2 : 0;
    var _tetherMin = isOriginSide ? _min : _offset - referenceRect[_len] - popperRect[_len] - _offsetModifierValue + normalizedTetherOffsetValue.altAxis;
    var _tetherMax = isOriginSide ? _offset + referenceRect[_len] + popperRect[_len] - _offsetModifierValue - normalizedTetherOffsetValue.altAxis : _max;
    var _preventedOffset = tether && isOriginSide ? withinMaxClamp(_tetherMin, _offset, _tetherMax) : within(tether ? _tetherMin : _min, _offset, tether ? _tetherMax : _max);
    popperOffsets2[altAxis] = _preventedOffset;
    data[altAxis] = _preventedOffset - _offset;
  }
  state.modifiersData[name] = data;
}
var preventOverflow_default = {
  name: "preventOverflow",
  enabled: true,
  phase: "main",
  fn: preventOverflow,
  requiresIfExists: ["offset"]
};

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getHTMLElementScroll.js
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getNodeScroll.js
function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/dom-utils/getCompositeRect.js
function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = round(rect.width) / element.offsetWidth || 1;
  var scaleY = round(rect.height) / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
}
function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  var isOffsetParentAnElement = isHTMLElement(offsetParent);
  var offsetParentIsScaled = isHTMLElement(offsetParent) && isElementScaled(offsetParent);
  var documentElement = getDocumentElement(offsetParent);
  var rect = getBoundingClientRect(elementOrVirtualElement, offsetParentIsScaled);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || // https://github.com/popperjs/popper-core/issues/1078
    isScrollParent(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      offsets = getBoundingClientRect(offsetParent, true);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/orderModifiers.js
function order(modifiers) {
  var map = /* @__PURE__ */ new Map();
  var visited = /* @__PURE__ */ new Set();
  var result = [];
  modifiers.forEach(function(modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function(dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function(modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function(acc, phase) {
    return acc.concat(orderedModifiers.filter(function(modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/debounce.js
function debounce(fn2) {
  var pending;
  return function() {
    if (!pending) {
      pending = new Promise(function(resolve) {
        Promise.resolve().then(function() {
          pending = void 0;
          resolve(fn2());
        });
      });
    }
    return pending;
  };
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/format.js
function format(str) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }
  return [].concat(args).reduce(function(p, c) {
    return p.replace(/%s/, c);
  }, str);
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/validateModifiers.js
var INVALID_MODIFIER_ERROR = 'Popper: modifier "%s" provided an invalid %s property, expected %s but got %s';
var MISSING_DEPENDENCY_ERROR = 'Popper: modifier "%s" requires "%s", but "%s" modifier is not available';
var VALID_PROPERTIES = ["name", "enabled", "phase", "fn", "effect", "requires", "options"];
function validateModifiers(modifiers) {
  modifiers.forEach(function(modifier) {
    [].concat(Object.keys(modifier), VALID_PROPERTIES).filter(function(value, index, self) {
      return self.indexOf(value) === index;
    }).forEach(function(key) {
      switch (key) {
        case "name":
          if (typeof modifier.name !== "string") {
            console.error(format(INVALID_MODIFIER_ERROR, String(modifier.name), '"name"', '"string"', '"' + String(modifier.name) + '"'));
          }
          break;
        case "enabled":
          if (typeof modifier.enabled !== "boolean") {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"enabled"', '"boolean"', '"' + String(modifier.enabled) + '"'));
          }
          break;
        case "phase":
          if (modifierPhases.indexOf(modifier.phase) < 0) {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"phase"', "either " + modifierPhases.join(", "), '"' + String(modifier.phase) + '"'));
          }
          break;
        case "fn":
          if (typeof modifier.fn !== "function") {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"fn"', '"function"', '"' + String(modifier.fn) + '"'));
          }
          break;
        case "effect":
          if (modifier.effect != null && typeof modifier.effect !== "function") {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"effect"', '"function"', '"' + String(modifier.fn) + '"'));
          }
          break;
        case "requires":
          if (modifier.requires != null && !Array.isArray(modifier.requires)) {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"requires"', '"array"', '"' + String(modifier.requires) + '"'));
          }
          break;
        case "requiresIfExists":
          if (!Array.isArray(modifier.requiresIfExists)) {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"requiresIfExists"', '"array"', '"' + String(modifier.requiresIfExists) + '"'));
          }
          break;
        case "options":
        case "data":
          break;
        default:
          console.error('PopperJS: an invalid property has been provided to the "' + modifier.name + '" modifier, valid properties are ' + VALID_PROPERTIES.map(function(s) {
            return '"' + s + '"';
          }).join(", ") + '; but "' + key + '" was provided.');
      }
      modifier.requires && modifier.requires.forEach(function(requirement) {
        if (modifiers.find(function(mod) {
          return mod.name === requirement;
        }) == null) {
          console.error(format(MISSING_DEPENDENCY_ERROR, String(modifier.name), requirement, requirement));
        }
      });
    });
  });
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/uniqueBy.js
function uniqueBy(arr, fn2) {
  var identifiers = /* @__PURE__ */ new Set();
  return arr.filter(function(item) {
    var identifier = fn2(item);
    if (!identifiers.has(identifier)) {
      identifiers.add(identifier);
      return true;
    }
  });
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/utils/mergeByName.js
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function(merged2, current) {
    var existing = merged2[current.name];
    merged2[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged2;
  }, {});
  return Object.keys(merged).map(function(key) {
    return merged[key];
  });
}

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/createPopper.js
var INVALID_ELEMENT_ERROR = "Popper: Invalid reference or popper argument provided. They must be either a DOM element or virtual element.";
var INFINITE_LOOP_ERROR = "Popper: An infinite loop in the modifiers cycle has been detected! The cycle has been interrupted to prevent a browser crash.";
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return !args.some(function(element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers3 = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function createPopper5(reference2, popper2, options) {
    if (options === void 0) {
      options = defaultOptions;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions),
      modifiersData: {},
      elements: {
        reference: reference2,
        popper: popper2
      },
      attributes: {},
      styles: {}
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance = {
      state,
      setOptions: function setOptions(setOptionsAction) {
        var options2 = typeof setOptionsAction === "function" ? setOptionsAction(state.options) : setOptionsAction;
        cleanupModifierEffects();
        state.options = Object.assign({}, defaultOptions, state.options, options2);
        state.scrollParents = {
          reference: isElement(reference2) ? listScrollParents(reference2) : reference2.contextElement ? listScrollParents(reference2.contextElement) : [],
          popper: listScrollParents(popper2)
        };
        var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers3, state.options.modifiers)));
        state.orderedModifiers = orderedModifiers.filter(function(m) {
          return m.enabled;
        });
        if (true) {
          var modifiers = uniqueBy([].concat(orderedModifiers, state.options.modifiers), function(_ref) {
            var name = _ref.name;
            return name;
          });
          validateModifiers(modifiers);
          if (getBasePlacement(state.options.placement) === auto) {
            var flipModifier = state.orderedModifiers.find(function(_ref2) {
              var name = _ref2.name;
              return name === "flip";
            });
            if (!flipModifier) {
              console.error(['Popper: "auto" placements require the "flip" modifier be', "present and enabled to work."].join(" "));
            }
          }
          var _getComputedStyle = getComputedStyle2(popper2), marginTop = _getComputedStyle.marginTop, marginRight = _getComputedStyle.marginRight, marginBottom = _getComputedStyle.marginBottom, marginLeft = _getComputedStyle.marginLeft;
          if ([marginTop, marginRight, marginBottom, marginLeft].some(function(margin) {
            return parseFloat(margin);
          })) {
            console.warn(['Popper: CSS "margin" styles cannot be used to apply padding', "between the popper and its reference element or boundary.", "To replicate margin, use the `offset` modifier, as well as", "the `padding` option in the `preventOverflow` and `flip`", "modifiers."].join(" "));
          }
        }
        runModifierEffects();
        return instance.update();
      },
      // Sync update – it will always be executed, even if not necessary. This
      // is useful for low frequency updates where sync behavior simplifies the
      // logic.
      // For high frequency updates (e.g. `resize` and `scroll` events), always
      // prefer the async Popper#update method
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements, reference3 = _state$elements.reference, popper3 = _state$elements.popper;
        if (!areValidElements(reference3, popper3)) {
          if (true) {
            console.error(INVALID_ELEMENT_ERROR);
          }
          return;
        }
        state.rects = {
          reference: getCompositeRect(reference3, getOffsetParent(popper3), state.options.strategy === "fixed"),
          popper: getLayoutRect(popper3)
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function(modifier) {
          return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
        });
        var __debug_loops__ = 0;
        for (var index = 0; index < state.orderedModifiers.length; index++) {
          if (true) {
            __debug_loops__ += 1;
            if (__debug_loops__ > 100) {
              console.error(INFINITE_LOOP_ERROR);
              break;
            }
          }
          if (state.reset === true) {
            state.reset = false;
            index = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index], fn2 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name = _state$orderedModifie.name;
          if (typeof fn2 === "function") {
            state = fn2({
              state,
              options: _options,
              name,
              instance
            }) || state;
          }
        }
      },
      // Async and optimistically optimized update – it will not be executed if
      // not necessary (debounced to run at most once-per-tick)
      update: debounce(function() {
        return new Promise(function(resolve) {
          instance.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      }
    };
    if (!areValidElements(reference2, popper2)) {
      if (true) {
        console.error(INVALID_ELEMENT_ERROR);
      }
      return instance;
    }
    instance.setOptions(options).then(function(state2) {
      if (!isDestroyed && options.onFirstUpdate) {
        options.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function(_ref3) {
        var name = _ref3.name, _ref3$options = _ref3.options, options2 = _ref3$options === void 0 ? {} : _ref3$options, effect4 = _ref3.effect;
        if (typeof effect4 === "function") {
          var cleanupFn = effect4({
            state,
            name,
            instance,
            options: options2
          });
          var noopFn = function noopFn2() {
          };
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function(fn2) {
        return fn2();
      });
      effectCleanupFns = [];
    }
    return instance;
  };
}
var createPopper2 = popperGenerator();

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/popper-lite.js
var defaultModifiers = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default];
var createPopper3 = popperGenerator({
  defaultModifiers
});

// node_modules/mdb-react-ui-kit/node_modules/@popperjs/core/lib/popper.js
var defaultModifiers2 = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default, offset_default, flip_default, preventOverflow_default, arrow_default, hide_default];
var createPopper4 = popperGenerator({
  defaultModifiers: defaultModifiers2
});

// node_modules/mdb-react-ui-kit/dist/mdb-react-ui-kit.esm.js
(function() {
  "use strict";
  try {
    if (typeof document != "undefined") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode(".dropdown-menu .active{color:#16181b;background-color:#eee}.dropdown-menu [data-active=true] a.dropdown-item,.dropdown-menu .dropdown-item:focus,.dropdown-menu li:focus .dropdown-item :not(.disabled){color:#16181b;background-color:#eee}.dropdown-menu li:focus{outline:none}.dropdown-menu.dropdown-menu-dark [data-active=true] a.dropdown-item,.dropdown-menu.dropdown-menu-dark .dropdown-item:focus,.dropdown-menu.dropdown-menu-dark li:focus .dropdown-item{color:#fff;background-color:#1266f1}.btn-group.dropstart>.dropdown-menu{right:0!important}")), document.head.appendChild(o);
    }
  } catch (d) {
    console.error("vite-plugin-css-injected-by-js", d);
  }
})();
var $e = import_react.default.forwardRef(
  ({ breakpoint: e, fluid: t, children: s, className: r, tag: n, ...a }, c) => {
    const o = clsx_m_default(`${t ? "container-fluid" : `container${e ? "-" + e : ""}`}`, r);
    return (0, import_jsx_runtime.jsx)(n, { className: o, ...a, ref: c, children: s });
  }
);
$e.defaultProps = { tag: "div" };
var Ee = import_react.default.forwardRef(
  ({
    center: e,
    children: t,
    className: s,
    end: r,
    lg: n,
    md: a,
    offsetLg: c,
    offsetMd: o,
    offsetSm: l,
    order: u,
    size: f,
    sm: m,
    start: d,
    tag: b,
    xl: g,
    xxl: h,
    xs: v,
    ...y
  }, $) => {
    const M = clsx_m_default(
      f && `col-${f}`,
      v && `col-xs-${v}`,
      m && `col-sm-${m}`,
      a && `col-md-${a}`,
      n && `col-lg-${n}`,
      g && `col-xl-${g}`,
      h && `col-xxl-${h}`,
      !f && !v && !m && !a && !n && !g && !h ? "col" : "",
      u && `order-${u}`,
      d && "align-self-start",
      e && "align-self-center",
      r && "align-self-end",
      l && `offset-sm-${l}`,
      o && `offset-md-${o}`,
      c && `offset-lg-${c}`,
      s
    );
    return (0, import_jsx_runtime.jsx)(b, { className: M, ref: $, ...y, children: t });
  }
);
Ee.defaultProps = { tag: "div" };
var Pe = import_react.default.forwardRef(
  ({ className: e, color: t, pill: s, light: r, dot: n, tag: a, children: c, notification: o, ...l }, u) => {
    const f = clsx_m_default(
      "badge",
      r ? t && `badge-${t}` : t && `bg-${t}`,
      n && "badge-dot",
      s && "rounded-pill",
      o && "badge-notification",
      e
    );
    return (0, import_jsx_runtime.jsx)(a, { className: f, ref: u, ...l, children: c });
  }
);
Pe.defaultProps = { tag: "span", color: "primary" };
var Ce = ({ ...e }) => {
  const [t, s] = (0, import_react.useState)(false), r = clsx_m_default("ripple-wave", t && "active");
  return (0, import_react.useEffect)(() => {
    const n = setTimeout(() => {
      s(true);
    }, 50);
    return () => {
      clearTimeout(n);
    };
  }, []), (0, import_jsx_runtime.jsx)("div", { className: r, ...e });
};
var Ie = (...e) => {
  const t = import_react.default.useRef();
  return import_react.default.useEffect(() => {
    e.forEach((s) => {
      s && (typeof s == "function" ? s(t.current) : s.current = t.current);
    });
  }, [e]), t;
};
var be = import_react.default.forwardRef(
  ({
    className: e,
    rippleTag: t,
    rippleCentered: s,
    rippleDuration: r,
    rippleUnbound: n,
    rippleRadius: a,
    rippleColor: c,
    children: o,
    onMouseDown: l,
    ...u
  }, f) => {
    const m = (0, import_react.useRef)(null), d = Ie(f, m), b = "rgba({{color}}, 0.2) 0, rgba({{color}}, 0.3) 40%, rgba({{color}}, 0.4) 50%, rgba({{color}}, 0.5) 60%, rgba({{color}}, 0) 70%", g = [0, 0, 0], h = ["primary", "secondary", "success", "danger", "warning", "info", "light", "dark"], [v, y] = (0, import_react.useState)([]), [$, M] = (0, import_react.useState)(false), C = clsx_m_default(
      "ripple",
      "ripple-surface",
      n && "ripple-surface-unbound",
      $ && `ripple-surface-${c}`,
      e
    ), N = () => {
      if (h.find((E) => E === (c == null ? void 0 : c.toLowerCase())))
        return M(true);
      {
        const E = S(c).join(",");
        return `radial-gradient(circle, ${b.split("{{color}}").join(`${E}`)})`;
      }
    }, S = (T) => {
      const E = (D) => (D.length < 7 && (D = `#${D[1]}${D[1]}${D[2]}${D[2]}${D[3]}${D[3]}`), [parseInt(D.substr(1, 2), 16), parseInt(D.substr(3, 2), 16), parseInt(D.substr(5, 2), 16)]), R = (D) => {
        const F = document.body.appendChild(document.createElement("fictum")), O = "rgb(1, 2, 3)";
        return F.style.color = O, F.style.color !== O || (F.style.color = D, F.style.color === O || F.style.color === "") ? g : (D = getComputedStyle(F).color, document.body.removeChild(F), D);
      }, W = (D) => (D = D.match(/[.\d]+/g).map((F) => +Number(F)), D.length = 3, D);
      return T.toLowerCase() === "transparent" ? g : T[0] === "#" ? E(T) : (T.indexOf("rgb") === -1 && (T = R(T)), T.indexOf("rgb") === 0 ? W(T) : g);
    }, I = (T) => {
      const { offsetX: E, offsetY: R, height: W, width: D } = T, F = R <= W / 2, O = E <= D / 2, L = (q, P) => Math.sqrt(q ** 2 + P ** 2), K = R === W / 2 && E === D / 2, Y = {
        first: F === true && O === false,
        second: F === true && O === true,
        third: F === false && O === true,
        fourth: F === false && O === false
      }, _ = {
        topLeft: L(E, R),
        topRight: L(D - E, R),
        bottomLeft: L(E, W - R),
        bottomRight: L(D - E, W - R)
      };
      let j = 0;
      return K || Y.fourth ? j = _.topLeft : Y.third ? j = _.topRight : Y.second ? j = _.bottomRight : Y.first && (j = _.bottomLeft), j * 2;
    }, X = (T) => {
      var j;
      const E = (j = d.current) == null ? void 0 : j.getBoundingClientRect(), R = T.clientX - E.left, W = T.clientY - E.top, D = E.height, F = E.width, O = {
        offsetX: s ? D / 2 : R,
        offsetY: s ? F / 2 : W,
        height: D,
        width: F
      }, L = {
        delay: r && r * 0.5,
        duration: r && r - r * 0.5
      }, K = I(O), Y = a || K / 2, _ = {
        left: s ? `${F / 2 - Y}px` : `${R - Y}px`,
        top: s ? `${D / 2 - Y}px` : `${W - Y}px`,
        height: a ? `${a * 2}px` : `${K}px`,
        width: a ? `${a * 2}px` : `${K}px`,
        transitionDelay: `0s, ${L.delay}ms`,
        transitionDuration: `${r}ms, ${L.duration}ms`
      };
      return $ ? _ : { ..._, backgroundImage: `${N()}` };
    }, A = (T) => {
      const E = X(T), R = v.concat(E);
      y(R), l && l(T);
    };
    return (0, import_react.useEffect)(() => {
      const T = setTimeout(() => {
        v.length > 0 && y(v.splice(1, v.length - 1));
      }, r);
      return () => {
        clearTimeout(T);
      };
    }, [r, v]), (0, import_jsx_runtime.jsxs)(t, { className: C, onMouseDown: (T) => A(T), ref: d, ...u, children: [
      o,
      v.map((T, E) => (0, import_jsx_runtime.jsx)(Ce, { style: T }, E))
    ] });
  }
);
be.defaultProps = { rippleTag: "div", rippleDuration: 500, rippleRadius: 0, rippleColor: "dark" };
var ae = import_react.default.forwardRef(
  ({
    className: e,
    color: t,
    outline: s,
    children: r,
    rounded: n,
    disabled: a,
    floating: c,
    size: o,
    href: l,
    block: u,
    active: f,
    toggle: m,
    noRipple: d,
    tag: b,
    ...g
  }, h) => {
    const [v, y] = (0, import_react.useState)(f || false);
    let $;
    const M = t && ["light", "link"].includes(t) || s ? "dark" : "light";
    t !== "none" ? s ? t ? $ = `btn-outline-${t}` : $ = "btn-outline-primary" : t ? $ = `btn-${t}` : $ = "btn-primary" : $ = "";
    const C = clsx_m_default(
      t !== "none" && "btn",
      $,
      n && "btn-rounded",
      c && "btn-floating",
      o && `btn-${o}`,
      `${(l || b !== "button") && a ? "disabled" : ""}`,
      u && "btn-block",
      v && "active",
      e
    );
    return l && b !== "a" && (b = "a"), ["hr", "img", "input"].includes(b) || d ? (0, import_jsx_runtime.jsx)(
      b,
      {
        className: C,
        onClick: m ? () => {
          y(!v);
        } : void 0,
        disabled: a && b === "button" ? true : void 0,
        href: l,
        ref: h,
        ...g,
        children: r
      }
    ) : (0, import_jsx_runtime.jsx)(
      be,
      {
        rippleTag: b,
        rippleColor: M,
        className: C,
        onClick: m ? () => {
          y(!v);
        } : void 0,
        disabled: a && b === "button" ? true : void 0,
        href: l,
        ref: h,
        ...g,
        children: r
      }
    );
  }
);
ae.defaultProps = { tag: "button", role: "button", color: "primary" };
var xe = import_react.default.forwardRef(
  ({ className: e, children: t, shadow: s, toolbar: r, size: n, vertical: a, tag: c, ...o }, l) => {
    let u;
    r ? u = "btn-toolbar" : a ? u = "btn-group-vertical" : u = "btn-group";
    const f = clsx_m_default(u, s && `shadow-${s}`, n && `btn-group-${n}`, e);
    return (0, import_jsx_runtime.jsx)(c, { className: f, ref: l, ...o, children: t });
  }
);
xe.defaultProps = { tag: "div", role: "group" };
var Le = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, color: r, grow: n, size: a, ...c }, o) => {
    const l = clsx_m_default(
      `${n ? "spinner-grow" : "spinner-border"}`,
      r && `text-${r}`,
      `${a ? n ? "spinner-grow-" + a : "spinner-border-" + a : ""}`,
      e
    );
    return (0, import_jsx_runtime.jsx)(s, { className: l, ref: o, ...c, children: t });
  }
);
Le.defaultProps = { tag: "div" };
var ke = import_react.default.forwardRef(
  ({ className: e, children: t, border: s, background: r, tag: n, shadow: a, alignment: c, ...o }, l) => {
    const u = clsx_m_default(
      "card",
      s && `border border-${s}`,
      r && `bg-${r}`,
      a && `shadow-${a}`,
      c && `text-${c}`,
      e
    );
    return (0, import_jsx_runtime.jsx)(n, { className: u, ref: l, ...o, children: t });
  }
);
ke.defaultProps = { tag: "div" };
var Oe = import_react.default.forwardRef(
  ({ className: e, children: t, border: s, background: r, tag: n, ...a }, c) => {
    const o = clsx_m_default("card-header", s && `border-${s}`, r && `bg-${r}`, e);
    return (0, import_jsx_runtime.jsx)(n, { className: o, ...a, ref: c, children: t });
  }
);
Oe.defaultProps = { tag: "div" };
var Ae = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("card-subtitle", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
Ae.defaultProps = { tag: "p" };
var Se = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("card-title", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
Se.defaultProps = { tag: "h5" };
var Fe = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("card-text", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
Fe.defaultProps = { tag: "p" };
var We = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("card-body", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
We.defaultProps = { tag: "div" };
var Xe = import_react.default.forwardRef(
  ({ className: e, children: t, border: s, background: r, tag: n, ...a }, c) => {
    const o = clsx_m_default("card-footer", s && `border-${s}`, r && `bg-${r}`, e);
    return (0, import_jsx_runtime.jsx)(n, { className: o, ...a, ref: c, children: t });
  }
);
Xe.defaultProps = { tag: "div" };
var Qt = ({ className: e, children: t, overlay: s, position: r, fluid: n, ...a }) => {
  const c = clsx_m_default(r && `card-img-${r}`, n && "img-fluid", s && "card-img", e);
  return (0, import_jsx_runtime.jsx)("img", { className: c, ...a, children: t });
};
var _e = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("card-img-overlay", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
_e.defaultProps = { tag: "div" };
var Zt = ({ className: e, children: t, ...s }) => {
  const r = clsx_m_default("card-link", e);
  return (0, import_jsx_runtime.jsx)("a", { className: r, ...s, children: t });
};
var je = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("card-group", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
je.defaultProps = { tag: "div" };
var Ye = import_react.default.forwardRef(
  ({ className: e, tag: t, horizontal: s, horizontalSize: r, light: n, numbered: a, children: c, small: o, ...l }, u) => {
    const f = clsx_m_default(
      "list-group",
      s && (r ? `list-group-horizontal-${r}` : "list-group-horizontal"),
      n && "list-group-light",
      a && "list-group-numbered",
      o && "list-group-small",
      e
    );
    return (0, import_jsx_runtime.jsx)(t, { className: f, ref: u, ...l, children: c });
  }
);
Ye.defaultProps = { tag: "ul" };
var Ve = import_react.default.forwardRef(
  ({ className: e, tag: t, active: s, disabled: r, action: n, color: a, children: c, noBorders: o, ...l }, u) => {
    const f = t === "button", m = clsx_m_default(
      "list-group-item",
      s && "active",
      r && !f && "disabled",
      n && "list-group-item-action",
      a && `list-group-item-${a}`,
      o && "border-0",
      e
    );
    return (0, import_jsx_runtime.jsx)(t, { className: m, disabled: f && r, ref: u, ...l, children: c });
  }
);
Ve.defaultProps = { tag: "li" };
var qe = ({
  className: e,
  children: t,
  disableMouseDown: s,
  tag: r,
  tooltipTag: n,
  options: a,
  placement: c,
  title: o,
  wrapperProps: l,
  wrapperClass: u,
  onShow: f,
  onHide: m,
  onMouseEnter: d,
  onMouseLeave: b,
  ...g
}) => {
  const [h, v] = (0, import_react.useState)(null), [y, $] = (0, import_react.useState)(null), [M, C] = (0, import_react.useState)(false), [N, S] = (0, import_react.useState)(false), [I, X] = (0, import_react.useState)(false), [A, T] = (0, import_react.useState)(false), E = clsx_m_default("tooltip", I && "show", "fade", `bs-tooltip-${c}`, e), { styles: R, attributes: W } = usePopper(h, y, {
    placement: c,
    ...a
  });
  (0, import_react.useEffect)(() => {
    let L, K;
    return M || N ? (T(true), L = setTimeout(() => {
      X(true);
    }, 4)) : (X(false), K = setTimeout(() => {
      T(false);
    }, 300)), () => {
      clearTimeout(L), clearTimeout(K);
    };
  }, [M, N]);
  const D = (L) => {
    f == null || f(L), !L.defaultPrevented && C(true), d == null || d(L);
  }, F = (L) => {
    m == null || m(L), !L.defaultPrevented && C(false), b == null || b(L);
  }, O = (0, import_react.useCallback)(
    (L) => {
      L.target === h ? S(true) : S(false);
    },
    [h]
  );
  return (0, import_react.useEffect)(() => {
    if (!s)
      return document.addEventListener("mousedown", O), () => {
        document.removeEventListener("mousedown", O);
      };
  }, [O, s]), (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      r,
      {
        className: u,
        onMouseEnter: D,
        onMouseLeave: F,
        ref: v,
        ...l,
        children: t
      }
    ),
    A && import_react_dom.default.createPortal(
      (0, import_jsx_runtime.jsx)(
        n,
        {
          ref: $,
          className: E,
          style: R.popper,
          ...W.popper,
          role: "tooltip",
          ...g,
          children: (0, import_jsx_runtime.jsx)("div", { className: "tooltip-inner", children: o })
        }
      ),
      document.body
    )
  ] });
};
qe.defaultProps = { tag: ae, tooltipTag: "div", placement: "top" };
var Ge = import_react.default.forwardRef(
  ({ around: e, between: t, bottom: s, center: r, children: n, className: a, evenly: c, end: o, middle: l, start: u, tag: f, top: m, ...d }, b) => {
    const g = clsx_m_default(
      "row",
      e && "justify-content-around",
      t && "justify-content-between",
      s && "align-self-end",
      r && "justify-content-center",
      c && "justifty-content-evenly",
      o && "justify-content-end",
      l && "align-self-center",
      u && "justify-content-start",
      m && "align-self-start",
      a
    );
    return (0, import_jsx_runtime.jsx)(f, { className: g, ...d, ref: b, children: n });
  }
);
Ge.defaultProps = { tag: "div" };
var zt = ({
  animate: e,
  className: t,
  icon: s,
  fab: r,
  fas: n,
  fal: a,
  far: c,
  flag: o,
  spin: l,
  fixed: u,
  flip: f,
  list: m,
  size: d,
  pull: b,
  pulse: g,
  color: h,
  border: v,
  rotate: y,
  inverse: $,
  stack: M,
  iconType: C,
  children: N,
  ...S
}) => {
  let I;
  o ? I = "flag" : r ? I = "fab" : n ? I = "fas" : c ? I = "far" : a ? I = "fal" : I = "fa";
  const X = clsx_m_default(
    C ? `fa-${C}` : I,
    e && `fa-${e}`,
    o ? `flag-${o}` : s && `fa-${s}`,
    d && `fa-${d}`,
    h && `text-${h}`,
    v && "fa-border",
    y && `fa-rotate-${y}`,
    b && `fa-pull-${b}`,
    l && !e && "fa-spin",
    m && "fa-li",
    u && "fa-fw",
    g && !e && "fa-pulse",
    $ && "fa-inverse",
    f && `fa-flip-${f}`,
    M && `fa-stack-${M}`,
    t
  );
  return (0, import_jsx_runtime.jsx)("i", { className: X, ...S, children: N });
};
var Ke = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, variant: r, color: n, blockquote: a, note: c, noteColor: o, listUnStyled: l, listInLine: u, ...f }, m) => {
    const d = clsx_m_default(
      r && r,
      a && "blockquote",
      c && "note",
      n && `text-${n}`,
      o && `note-${o}`,
      l && "list-unstyled",
      u && "list-inline",
      e
    );
    return a && (s = "blockquote"), (l || u) && (s = "ul"), (0, import_jsx_runtime.jsx)(s, { className: d, ref: m, ...f, children: t });
  }
);
Ke.defaultProps = { tag: "p" };
var Ht = import_react.default.forwardRef(
  ({ className: e, color: t, uppercase: s, bold: r, children: n, ...a }, c) => {
    const o = clsx_m_default(
      "breadcrumb",
      r && "font-weight-bold",
      t && `text-${t}`,
      s && "text-uppercase",
      e
    );
    return (0, import_jsx_runtime.jsx)("nav", { "aria-label": "breadcrumb", children: (0, import_jsx_runtime.jsx)("ol", { className: o, ref: c, ...a, children: n }) });
  }
);
var Ue = import_react.default.forwardRef(
  ({ className: e, active: t, current: s, children: r, ...n }, a) => {
    const c = clsx_m_default("breadcrumb-item", t && "active", e);
    return (0, import_jsx_runtime.jsx)("li", { className: c, ref: a, "aria-current": t && s, ...n, children: r });
  }
);
Ue.defaultProps = {
  current: "page"
};
var Je = (e) => {
  if (e !== false)
    return `navbar-expand-${e}`;
};
var Qe = import_react.default.forwardRef(
  ({
    className: e,
    children: t,
    light: s,
    dark: r,
    scrolling: n,
    fixed: a,
    sticky: c,
    scrollingNavbarOffset: o,
    color: l,
    transparent: u,
    expand: f,
    tag: m,
    bgColor: d,
    ...b
  }, g) => {
    const [h, v] = (0, import_react.useState)(false), y = clsx_m_default(
      {
        "navbar-light": s,
        "navbar-dark": r,
        "scrolling-navbar": n || o,
        "top-nav-collapse": h,
        [`text-${l}`]: l && u ? h : l
      },
      a && `fixed-${a}`,
      c && "sticky-top",
      "navbar",
      f && Je(f),
      d && `bg-${d}`,
      e
    ), $ = (0, import_react.useCallback)(() => {
      o && window.pageYOffset > o ? v(true) : v(false);
    }, [o]);
    return (0, import_react.useEffect)(() => ((n || o) && window.addEventListener("scroll", $), () => {
      window.removeEventListener("scroll", $);
    }), [$, n, o]), (0, import_jsx_runtime.jsx)(m, { className: y, role: "navigation", ...b, ref: g, children: t });
  }
);
Qe.defaultProps = {
  tag: "nav"
};
var Ze = import_react.default.forwardRef(
  ({ children: e, className: t, disabled: s, active: r, tag: n, ...a }, c) => {
    const o = clsx_m_default("nav-link", s ? "disabled" : r ? "active" : "", t);
    return (0, import_jsx_runtime.jsx)(n, { "data-test": "nav-link", className: o, style: { cursor: "pointer" }, ref: c, ...a, children: e });
  }
);
Ze.defaultProps = {
  tag: "a",
  active: false,
  className: "",
  disabled: false
};
var ze = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("navbar-brand", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ref: n, ...r, children: t });
  }
);
ze.defaultProps = {
  tag: "a"
};
var He = import_react.default.forwardRef(
  ({ children: e, className: t, active: s, text: r, tag: n, ...a }, c) => {
    const o = clsx_m_default("nav-item", s && "active", r && "navbar-text", t);
    return (0, import_jsx_runtime.jsx)(n, { ...a, className: o, ref: c, children: e });
  }
);
He.defaultProps = {
  tag: "li"
};
var et = import_react.default.forwardRef(
  ({ children: e, className: t, right: s, fullWidth: r, left: n, tag: a, ...c }, o) => {
    const l = clsx_m_default("navbar-nav", r && "w-100", s && "ms-auto", n && "me-auto", t);
    return (0, import_jsx_runtime.jsx)(a, { className: l, ref: o, ...c, children: e });
  }
);
et.defaultProps = {
  tag: "ul",
  fullWidth: true
};
var tt = import_react.default.forwardRef(
  ({ children: e, className: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("navbar-toggler", t);
    return (0, import_jsx_runtime.jsx)(s, { ...r, className: a, ref: n, children: e });
  }
);
tt.defaultProps = {
  tag: "button"
};
var es = import_react.default.forwardRef(
  ({ children: e, bgColor: t, color: s, className: r, ...n }, a) => {
    const c = clsx_m_default(t && `bg-${t}`, s && `text-${s}`, r);
    return (0, import_jsx_runtime.jsx)("footer", { className: c, ...n, ref: a, children: e });
  }
);
var ts = import_react.default.forwardRef(
  ({ children: e, size: t, circle: s, center: r, end: n, start: a, className: c, ...o }, l) => {
    const u = clsx_m_default(
      "pagination",
      r && "justify-content-center",
      s && "pagination-circle",
      n && "justify-content-end",
      t && `pagination-${t}`,
      a && "justify-content-start",
      c
    );
    return (0, import_jsx_runtime.jsx)("ul", { className: u, ...o, ref: l, children: e });
  }
);
var st = import_react.default.forwardRef(
  ({ children: e, className: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("page-link", t);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: e });
  }
);
st.defaultProps = {
  tag: "a"
};
var ss = import_react.default.forwardRef(
  ({ children: e, className: t, active: s, disabled: r, ...n }, a) => {
    const c = clsx_m_default("page-item", s && "active", r && "disabled", t);
    return (0, import_jsx_runtime.jsx)("li", { className: c, ...n, ref: a, children: e });
  }
);
var rs = ({
  className: e,
  classNameResponsive: t,
  responsive: s,
  align: r,
  borderColor: n,
  bordered: a,
  borderless: c,
  children: o,
  color: l,
  hover: u,
  small: f,
  striped: m,
  ...d
}) => {
  const b = clsx_m_default(
    "table",
    r && `align-${r}`,
    n && `border-${n}`,
    a && "table-bordered",
    c && "table-borderless",
    l && `table-${l}`,
    u && "table-hover",
    f && "table-sm",
    m && "table-striped",
    e
  ), g = (0, import_react.useMemo)(() => (0, import_jsx_runtime.jsx)("table", { className: b, ...d, children: o }), [o, b, d]);
  if (s) {
    const h = clsx_m_default(
      typeof s == "string" ? `table-responsive-${s}` : "table-responsive",
      t
    );
    return (0, import_jsx_runtime.jsx)("div", { className: h, children: g });
  } else
    return g;
};
var ns = ({ className: e, children: t, dark: s, light: r, ...n }) => {
  const a = clsx_m_default(s && "table-dark", r && "table-light", e);
  return (0, import_jsx_runtime.jsx)("thead", { className: a, ...n, children: t });
};
var as = ({ className: e, children: t, ...s }) => {
  const r = clsx_m_default(e);
  return (0, import_jsx_runtime.jsx)("tbody", { className: r, ...s, children: t });
};
var ve = import_react.default.forwardRef(
  ({ animated: e, children: t, className: s, style: r, tag: n, valuenow: a, valuemax: c, striped: o, bgColor: l, valuemin: u, width: f, ...m }, d) => {
    const b = clsx_m_default(
      "progress-bar",
      l && `bg-${l}`,
      o && "progress-bar-striped",
      e && "progress-bar-animated",
      s
    ), g = { width: `${f}%`, ...r };
    return (0, import_jsx_runtime.jsx)(
      n,
      {
        className: b,
        style: g,
        ref: d,
        role: "progressbar",
        ...m,
        "aria-valuenow": Number(f) ?? a,
        "aria-valuemin": Number(u),
        "aria-valuemax": Number(c),
        children: t
      }
    );
  }
);
ve.defaultProps = { tag: "div" };
var rt = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, height: r, style: n, ...a }, c) => {
    const o = clsx_m_default("progress", e), l = { height: `${r}px`, ...n };
    return (0, import_jsx_runtime.jsx)(s, { className: o, ref: c, style: l, ...a, children: import_react.default.Children.map(t, (u) => {
      if (!import_react.default.isValidElement(u) || u.type !== ve) {
        console.error("Progress component only allows ProgressBar as child");
        return;
      } else
        return u;
    }) });
  }
);
rt.defaultProps = { tag: "div" };
var nt = import_react.default.forwardRef(
  ({
    className: e,
    size: t,
    contrast: s,
    value: r,
    defaultValue: n,
    id: a,
    labelClass: c,
    wrapperClass: o,
    wrapperStyle: l,
    wrapperTag: u,
    label: f,
    onChange: m,
    children: d,
    labelRef: b,
    labelStyle: g,
    type: h,
    onBlur: v,
    readonly: y,
    ...$
  }, M) => {
    var Y, _;
    const [C, N] = (0, import_react.useState)(r || n), [S, I] = (0, import_react.useState)(0), [X, A] = (0, import_react.useState)(false), T = (0, import_react.useRef)(null);
    (0, import_react.useImperativeHandle)(M, () => T.current);
    const E = (0, import_react.useRef)(null), R = b || E, W = clsx_m_default("form-outline", s && "form-white", o), D = clsx_m_default(
      "form-control",
      X && "active",
      h === "date" && "active",
      t && `form-control-${t}`,
      e
    ), F = clsx_m_default("form-label", c);
    (0, import_react.useEffect)(() => {
      if (!T.current)
        return;
      const { value: j } = T.current;
      j != "" ? A(true) : A(false);
    }, [(Y = T.current) == null ? void 0 : Y.value]), (0, import_react.useEffect)(() => {
      r !== void 0 && (r != "" ? A(true) : A(false));
    }, [r]), (0, import_react.useEffect)(() => {
      n !== void 0 && (n != "" ? A(true) : A(false));
    }, [n]);
    const O = (0, import_react.useCallback)(() => {
      var j;
      (j = R.current) != null && j.clientWidth && I(R.current.clientWidth * 0.8 + 8);
    }, [R]);
    (0, import_react.useEffect)(() => {
      O();
    }, [(_ = R.current) == null ? void 0 : _.clientWidth, O]);
    const L = (j) => {
      N(j.target.value), m == null || m(j);
    }, K = (0, import_react.useCallback)(
      (j) => {
        T.current && (C !== void 0 && C != "" || r !== void 0 && r != "" || T.current.value != "" ? A(true) : A(false), v && v(j));
      },
      [C, r, v]
    );
    return (0, import_jsx_runtime.jsxs)(u, { className: W, style: l, children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: h,
          readOnly: y,
          className: D,
          onBlur: K,
          onChange: L,
          onFocus: O,
          value: r,
          defaultValue: n,
          id: a,
          ref: T,
          ...$
        }
      ),
      f && (0, import_jsx_runtime.jsx)("label", { className: F, style: g, htmlFor: a, ref: R, children: f }),
      (0, import_jsx_runtime.jsxs)("div", { className: "form-notch", children: [
        (0, import_jsx_runtime.jsx)("div", { className: "form-notch-leading" }),
        (0, import_jsx_runtime.jsx)("div", { className: "form-notch-middle", style: { width: S } }),
        (0, import_jsx_runtime.jsx)("div", { className: "form-notch-trailing" })
      ] }),
      d
    ] });
  }
);
nt.defaultProps = { wrapperTag: "div", readonly: false };
var oe = ({
  className: e,
  inputRef: t,
  labelClass: s,
  wrapperClass: r,
  labelStyle: n,
  wrapperTag: a,
  wrapperStyle: c,
  label: o,
  inline: l,
  btn: u,
  id: f,
  btnColor: m,
  disableWrapper: d,
  toggleSwitch: b,
  ...g
}) => {
  let h = "form-check-input", v = "form-check-label";
  u && (h = "btn-check", m ? v = `btn btn-${m}` : v = "btn btn-primary");
  const y = clsx_m_default(
    o && !u && "form-check",
    l && !u && "form-check-inline",
    b && "form-switch",
    r
  ), $ = clsx_m_default(h, e), M = clsx_m_default(v, s), C = (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("input", { className: $, id: f, ref: t, ...g }),
    o && (0, import_jsx_runtime.jsx)("label", { className: M, style: n, htmlFor: f, children: o })
  ] });
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: d ? C : (0, import_jsx_runtime.jsx)(a, { style: c, className: y, children: C }) });
};
oe.defaultProps = { wrapperTag: "div" };
var os = ({ ...e }) => (0, import_jsx_runtime.jsx)(oe, { type: "checkbox", ...e });
var cs = ({ ...e }) => (0, import_jsx_runtime.jsx)(oe, { type: "radio", ...e });
var he = ({
  className: e,
  children: t,
  show: s = false,
  id: r,
  navbar: n,
  tag: a,
  collapseRef: c,
  style: o,
  onShow: l,
  onHide: u,
  ...f
}) => {
  const [m, d] = (0, import_react.useState)(false), [b, g] = (0, import_react.useState)(void 0), [h, v] = (0, import_react.useState)(false), y = clsx_m_default(
    h ? "collapsing" : "collapse",
    !h && m && "show",
    n && "navbar-collapse",
    e
  ), $ = (0, import_react.useRef)(null), M = c ?? $, C = (0, import_react.useCallback)(() => {
    m && g(void 0);
  }, [m]);
  return (0, import_react.useEffect)(() => {
    var N;
    b === void 0 && m && g((N = M == null ? void 0 : M.current) == null ? void 0 : N.scrollHeight);
  }, [b, m, M]), (0, import_react.useEffect)(() => {
    m !== s && (s ? l == null || l() : u == null || u(), d(s)), m && v(true);
    const N = setTimeout(() => {
      v(false);
    }, 350);
    return () => {
      clearTimeout(N);
    };
  }, [s, m, l, u]), (0, import_react.useEffect)(() => {
    var N;
    g(m ? (N = M == null ? void 0 : M.current) == null ? void 0 : N.scrollHeight : 0);
  }, [m, M]), (0, import_react.useEffect)(() => (window.addEventListener("resize", C), () => {
    window.removeEventListener("resize", C);
  }), [C]), (0, import_jsx_runtime.jsx)(a, { style: { height: b, ...o }, id: r, className: y, ...f, ref: M, children: t });
};
he.defaultProps = { tag: "div" };
var ye = (0, import_react.createContext)(null);
var at = ({
  children: e,
  isOpen: t = false,
  options: s,
  animation: r = true,
  dropup: n,
  dropright: a,
  dropleft: c,
  onHide: o,
  onShow: l
}) => {
  const [u, f] = (0, import_react.useState)(t), [m, d] = (0, import_react.useState)(null), [b, g] = (0, import_react.useState)(null), [h, v] = (0, import_react.useState)(-1);
  return (0, import_jsx_runtime.jsx)(
    ye.Provider,
    {
      value: {
        animation: r,
        activeIndex: h,
        isOpenState: u,
        setReferenceElement: d,
        setPopperElement: g,
        setActiveIndex: v,
        popperElement: b,
        setIsOpenState: f,
        referenceElement: m,
        onHide: o,
        onShow: l,
        dropup: n,
        options: s,
        dropright: a,
        dropleft: c
      },
      children: e
    }
  );
};
var ot = (e) => e instanceof HTMLElement;
var ct = (e) => e instanceof Node;
var te = () => {
  const e = (0, import_react.useContext)(ye);
  if (!e)
    throw new Error("Missing context data");
  return e;
};
var lt = () => {
  const { isOpenState: e, setIsOpenState: t, setActiveIndex: s, popperElement: r, referenceElement: n, onHide: a } = te(), c = (0, import_react.useCallback)(
    (o) => {
      a == null || a(o), !(!e || !ct(o.target) || r && r.contains(o.target) || n && n.contains(o.target) || o.defaultPrevented) && (t(false), setTimeout(() => s(-1), 300));
    },
    [e, t, s, r, n, a]
  );
  (0, import_react.useEffect)(() => (document.addEventListener("mousedown", c), () => document.removeEventListener("mousedown", c)), [c]);
};
var it = ({
  className: e,
  tag: t = "div",
  group: s,
  children: r,
  dropup: n,
  dropright: a,
  dropleft: c,
  ...o
}) => {
  lt();
  const l = clsx_m_default(
    s ? "btn-group" : "dropdown",
    n && "dropup",
    a && "dropend",
    c && "dropstart",
    e
  );
  return (0, import_jsx_runtime.jsx)(t, { className: l, ...o, children: r });
};
var ls = ({ animation: e, onHide: t, onShow: s, ...r }) => (0, import_jsx_runtime.jsx)(at, { animation: e, onHide: t, onShow: s, ...r, children: (0, import_jsx_runtime.jsx)(it, { ...r }) });
var ut = ({
  childTag: e,
  children: t,
  disabled: s,
  link: r,
  divider: n,
  header: a,
  href: c = "#"
}) => {
  const o = clsx_m_default("dropdown-item", s && "disabled");
  return r ? e ? (0, import_jsx_runtime.jsx)(e, { className: o, children: t }) : (0, import_jsx_runtime.jsx)("a", { href: c, className: o, children: t }) : n ? e ? (0, import_jsx_runtime.jsx)(e, { className: "dropdown-divider", children: t }) : (0, import_jsx_runtime.jsx)("hr", { className: "dropdown-divider" }) : a ? e ? (0, import_jsx_runtime.jsx)(e, { className: "dropdown-header", children: t }) : (0, import_jsx_runtime.jsx)("h6", { className: "dropdown-header", children: t }) : (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t });
};
var is = ({
  onClick: e,
  tag: t = "li",
  childTag: s,
  children: r,
  style: n,
  link: a,
  divider: c,
  header: o,
  disabled: l,
  href: u,
  preventCloseOnClick: f,
  ...m
}) => {
  const { setIsOpenState: d, onHide: b, setActiveIndex: g } = te();
  return (0, import_jsx_runtime.jsx)(t, { style: n, onClick: (v) => {
    b == null || b(v), e == null || e(v), !(l || f || v.defaultPrevented) && (setTimeout(() => g(-1), 300), d(false));
  }, ...m, children: (0, import_jsx_runtime.jsx)(
    ut,
    {
      link: a,
      divider: c,
      header: o,
      disabled: l,
      href: u,
      childTag: s,
      children: r
    }
  ) });
};
var fe = (e, t, s) => s === "up" ? e <= 0 ? t[t.length - 1].props.divider === true || t[t.length - 1].props.disabled === true : t[e - 1].props.divider === true || t[e - 1].props.disabled === true : e === t.length - 1 ? t[0].props.divider === true || t[0].props.disabled === true : t[e + 1].props.divider === true || t[e + 1].props.disabled === true;
var dt = (e) => {
  const { activeIndex: t, isOpenState: s, setIsOpenState: r, setActiveIndex: n, onHide: a } = te(), c = (0, import_react.useCallback)(
    (o) => {
      if (Array.isArray(e)) {
        if (ot(document.activeElement) && document.activeElement.blur(), o.key === "ArrowUp") {
          o.preventDefault();
          const l = fe(t, e, "up");
          if (t === 1) {
            n(l ? e.length - 1 : 0);
            return;
          }
          if (t <= 0) {
            n(l ? e.length - 2 : e.length - 1);
            return;
          }
          n((u) => l ? u - 2 : u - 1);
        }
        if (o.key === "ArrowDown" || o.key === "Tab") {
          o.preventDefault();
          const l = fe(t, e, "down");
          if (t === e.length - 2) {
            n((u) => l ? 0 : u + 1);
            return;
          }
          if (t === e.length - 1) {
            n(l ? 1 : 0);
            return;
          }
          n((u) => l ? u + 2 : u + 1);
        }
        if (o.key === "Enter") {
          const l = document.querySelector('[data-active="true"]'), u = l == null ? void 0 : l.firstElementChild;
          if (u)
            return u.click();
          if (a == null || a(o), o.defaultPrevented)
            return;
          r(false), setTimeout(() => n(-1), 300);
        }
        if (o.key === "Escape") {
          if (a == null || a(o), o.defaultPrevented)
            return;
          r(false), setTimeout(() => n(-1), 300);
        }
      }
    },
    [e, r, n, t, a]
  );
  (0, import_react.useEffect)(() => (s && document.addEventListener("keydown", c), () => {
    s && document.removeEventListener("keydown", c);
  }), [s, c]), (0, import_react.useEffect)(() => {
    const o = document.querySelector('[data-active="true"]'), l = o == null ? void 0 : o.firstElementChild;
    return l == null || l.focus(), () => l == null ? void 0 : l.blur();
  }, [t]);
};
var ft = () => {
  const { isOpenState: e } = te(), [t, s] = (0, import_react.useState)(false), [r, n] = (0, import_react.useState)(false), [a, c] = (0, import_react.useState)(e);
  return (0, import_react.useEffect)(() => {
    let o;
    return e || (n(true), s(false), o = setTimeout(() => {
      n(false), c(false);
    }, 300)), e && (s(true), n(false), c(true), o = setTimeout(() => {
      s(false);
    }, 300)), () => clearTimeout(o);
  }, [e]), { show: a, isFadeIn: t, isFadeOut: r };
};
var us = ({
  className: e,
  tag: t = "ul",
  children: s,
  style: r,
  dark: n,
  responsive: a = "",
  appendToBody: c = false,
  alwaysOpen: o,
  ...l
}) => {
  const {
    activeIndex: u,
    setPopperElement: f,
    isOpenState: m,
    animation: d,
    referenceElement: b,
    popperElement: g,
    options: h,
    dropleft: v,
    dropup: y,
    dropright: $
  } = te(), { show: M, isFadeIn: C, isFadeOut: N } = ft();
  dt(s);
  const S = () => {
    if ($)
      return "right-start";
    if (v)
      return "left-start";
    const T = g && getComputedStyle(g).getPropertyValue("--mdb-position").trim() === "end";
    return y ? T ? "top-end" : "top-start" : T ? "bottom-end" : "bottom-start";
  }, { styles: I } = usePopper(b, g, {
    placement: S(),
    modifiers: [flip_default],
    ...h
  }), X = clsx_m_default(
    "dropdown-menu",
    n && "dropdown-menu-dark",
    m && "show",
    d && "animation",
    C && "fade-in",
    N && "fade-out",
    a && `dropdown-menu-${a}`,
    e
  );
  if (!M && !o)
    return null;
  const A = (0, import_jsx_runtime.jsx)(
    t,
    {
      className: X,
      style: { position: "absolute", zIndex: 1e3, ...I.popper, ...r },
      ref: f,
      ...l,
      children: import_react.Children.map(
        s,
        (T, E) => (0, import_react.cloneElement)(T, {
          tabIndex: 1,
          "data-active": u === E && true,
          className: clsx_m_default(u === E ? "active" : "", T.props.className)
        })
      )
    }
  );
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: c ? (0, import_react_dom.createPortal)(A, document.body) : A });
};
var ds = ({
  className: e,
  tag: t = ae,
  children: s,
  onClick: r,
  split: n,
  ...a
}) => {
  const { setIsOpenState: c, setReferenceElement: o, isOpenState: l, setActiveIndex: u, onHide: f, onShow: m } = te(), d = clsx_m_default("dropdown-toggle", n && "dropdown-toggle-split", e);
  return (0, import_jsx_runtime.jsx)(
    t,
    {
      onClick: (g) => {
        r == null || r(g), l ? f == null || f(g) : m == null || m(g), !g.defaultPrevented && (c((h) => !h), setTimeout(() => u(-1), 300));
      },
      ref: o,
      className: d,
      "aria-expanded": !!l,
      ...a,
      children: s
    }
  );
};
var mt = ({
  className: e,
  btnClassName: t,
  btnChildren: s,
  children: r,
  tag: n,
  onShow: a,
  onHide: c,
  popperTag: o,
  isOpen: l,
  placement: u,
  dismiss: f,
  options: m,
  poperStyle: d,
  onClick: b,
  ...g
}) => {
  const [h, v] = (0, import_react.useState)(), [y, $] = (0, import_react.useState)(), { styles: M, attributes: C } = usePopper(h, y, { placement: u, ...m }), [N, S] = (0, import_react.useState)(l ?? false), [I, X] = (0, import_react.useState)(false), [A, T] = (0, import_react.useState)(false), E = clsx_m_default(
    "popover fade",
    I && N && "show",
    `bs-popover-${u === "left" ? "start" : u === "right" ? "end" : u}`,
    e
  ), R = (D) => {
    N && !f ? c == null || c() : N || a == null || a(), f ? (T(true), S(true)) : S(!N), b && b(D);
  };
  (0, import_react.useEffect)(() => {
    l || S(false);
  }, [l]);
  const W = (0, import_react.useCallback)(
    (D) => {
      A && y && y !== null && N && h && h !== null && (h.contains(D.target) || (S(false), c == null || c()));
    },
    [A, N, y, h, c]
  );
  return (0, import_react.useEffect)(() => {
    const D = setTimeout(() => {
      X(N);
    }, 150);
    return () => {
      clearTimeout(D);
    };
  }, [N]), (0, import_react.useEffect)(() => (N && document.addEventListener("mousedown", W), () => {
    document.removeEventListener("mousedown", W);
  }), [W, N]), (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(n, { onClick: R, className: t, ...g, ref: v, children: s }),
    (I || N) && import_react_dom.default.createPortal(
      (0, import_jsx_runtime.jsx)(
        o,
        {
          className: E,
          ref: $,
          style: { ...M.popper, ...d },
          ...C.popper,
          children: r
        }
      ),
      document.body
    )
  ] });
};
mt.defaultProps = {
  tag: ae,
  popperTag: "div",
  placement: "bottom"
};
var pt = ({ className: e, children: t, tag: s, ...r }) => {
  const n = clsx_m_default("popover-body", e);
  return (0, import_jsx_runtime.jsx)(s, { className: n, ...r, children: t });
};
pt.defaultProps = {
  tag: "div"
};
var gt = ({ className: e, children: t, tag: s, ...r }) => {
  const n = clsx_m_default("popover-header", e);
  return (0, import_jsx_runtime.jsx)(s, { className: n, ...r, children: t });
};
gt.defaultProps = {
  tag: "h3"
};
var fs = ({
  animationDirection: e,
  appendToBody: t,
  backdrop: s = true,
  children: r,
  className: n,
  closeOnEsc: a = true,
  setShow: c,
  leaveHiddenModal: o = true,
  modalRef: l,
  onHide: u,
  onHidePrevented: f,
  onShow: m,
  show: d,
  staticBackdrop: b,
  nonInvasive: g = false,
  tag: h = "div",
  ...v
}) => {
  const [y, $] = (0, import_react.useState)(d), [M, C] = (0, import_react.useState)(d), [N, S] = (0, import_react.useState)(d), [I, X] = (0, import_react.useState)(false), [A, T] = (0, import_react.useState)(0), [E, R] = (0, import_react.useState)([]), W = (0, import_react.useRef)(null), D = l || W, F = clsx_m_default(
    "modal",
    I && "modal-static",
    e,
    "fade",
    M && "show",
    y && g && "modal-non-invasive-show",
    n
  ), O = clsx_m_default("modal-backdrop", "fade", y && "show"), L = (0, import_react.useCallback)(() => {
    C((q) => (q && (u == null || u()), false)), setTimeout(() => {
      $(false), c == null || c(false);
    }, 150), setTimeout(() => {
      S(false);
    }, 350);
  }, [u, c]), K = (0, import_react.useCallback)(
    (q) => {
      g || M && q.target === D.current && (b ? (X(true), f == null || f(), setTimeout(() => {
        X(false);
      }, 300)) : L());
    },
    [M, D, b, L, f, g]
  ), Y = (0, import_react.useCallback)(
    (q) => {
      M && q.key === "Tab" && (q.preventDefault(), T(A + 1)), a && M && q.key === "Escape" && (b ? (X(true), f == null || f(), setTimeout(() => {
        X(false);
      }, 300)) : L());
    },
    [M, a, A, b, L, f]
  );
  (0, import_react.useEffect)(() => {
    var k;
    const q = (k = D.current) == null ? void 0 : k.querySelectorAll(
      "button, a, input, select, textarea, [tabindex]"
    ), P = Array.from(q).filter((U) => U.tabIndex !== -1).sort((U, Z) => U.tabIndex === Z.tabIndex ? 0 : Z.tabIndex === null ? -1 : U.tabIndex === null ? 1 : U.tabIndex - Z.tabIndex);
    R(P), T(P.length - 1);
  }, [D]), (0, import_react.useEffect)(() => {
    E && E.length > 0 && (A === E.length ? (E[0].focus(), T(0)) : E[A].focus());
  }, [A, E]), (0, import_react.useEffect)(() => {
    const q = () => {
      const k = document.documentElement.clientWidth;
      return Math.abs(window.innerWidth - k);
    }, P = window.innerWidth > document.documentElement.clientWidth && window.innerWidth >= 576;
    if (N && P && !g) {
      const k = q();
      document.body.classList.add("modal-open"), document.body.style.overflow = "hidden", document.body.style.paddingRight = `${k}px`;
    } else
      document.body.classList.remove("modal-open"), document.body.style.overflow = "", document.body.style.paddingRight = "";
    return () => {
      document.body.classList.remove("modal-open"), document.body.style.overflow = "", document.body.style.paddingRight = "";
    };
  }, [N, g]), (0, import_react.useEffect)(() => {
    d ? (m == null || m(), S(true), setTimeout(() => {
      $(true);
    }, 0), setTimeout(() => {
      C(true), c == null || c(true);
    }, 150)) : L();
  }, [d, L, c, m]), (0, import_react.useEffect)(() => {
    const q = (P) => {
      P.target.closest(".modal-dialog") || window.addEventListener("mouseup", K, { once: true });
    };
    return window.addEventListener("mousedown", q), window.addEventListener("keydown", Y), () => {
      window.removeEventListener("mousedown", q), window.removeEventListener("keydown", Y);
    };
  }, [Y, K]);
  const _ = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: (o || d || N) && import_react_dom.default.createPortal(
    (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(
        h,
        {
          className: F,
          ref: D,
          style: { display: N || d ? "block" : "none", pointerEvents: g ? "none" : "initial" },
          ...v,
          children: r
        }
      ),
      import_react_dom.default.createPortal(
        s && N && !g && (0, import_jsx_runtime.jsx)("div", { className: O }),
        document.body
      )
    ] }),
    document.body
  ) }), j = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: (o || d || N) && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      h,
      {
        className: F,
        ref: D,
        style: { display: N || d ? "block" : "none", pointerEvents: g ? "none" : "initial" },
        ...v,
        children: r
      }
    ),
    import_react_dom.default.createPortal(
      s && N && !g && (0, import_jsx_runtime.jsx)("div", { onClick: L, className: O }),
      document.body
    )
  ] }) });
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t ? _ : j });
};
var bt = import_react.default.forwardRef(
  ({ className: e, centered: t, children: s, size: r, scrollable: n, tag: a, ...c }, o) => {
    const l = clsx_m_default(
      "modal-dialog",
      n && "modal-dialog-scrollable",
      t && "modal-dialog-centered",
      r && `modal-${r}`,
      e
    );
    return (0, import_jsx_runtime.jsx)(a, { className: l, ...c, ref: o, children: s });
  }
);
bt.defaultProps = { tag: "div" };
var vt = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("modal-content", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
vt.defaultProps = { tag: "div" };
var ht = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("modal-header", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
ht.defaultProps = { tag: "div" };
var yt = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("modal-title", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
yt.defaultProps = { tag: "h5" };
var Nt = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("modal-body", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
Nt.defaultProps = { tag: "div" };
var wt = import_react.default.forwardRef(
  ({ className: e, children: t, tag: s, ...r }, n) => {
    const a = clsx_m_default("modal-footer", e);
    return (0, import_jsx_runtime.jsx)(s, { className: a, ...r, ref: n, children: t });
  }
);
wt.defaultProps = { tag: "div" };
var ie = import_react.default.createContext({
  activeElement: null,
  setTargets: null
});
var ms = ({
  container: e = typeof window !== void 0 ? window : null,
  className: t,
  children: s,
  offset: r = 10,
  ...n
}) => {
  const a = clsx_m_default("sticky-top", t), [c, o] = (0, import_react.useState)(null), [l, u] = (0, import_react.useState)([]), f = e instanceof Window, m = (0, import_react.useCallback)(() => {
    var y, $, M;
    if (!l.length)
      return;
    const d = f ? window.pageYOffset : (y = e == null ? void 0 : e.current) == null ? void 0 : y.scrollTop, b = Number(r), g = ($ = l[l.length - 1]) == null ? void 0 : $.current, h = (M = l[0]) == null ? void 0 : M.current;
    d + b < h.offsetTop && o(null), l.forEach((C, N) => {
      var A;
      const S = (A = l[N + 1]) == null ? void 0 : A.current, I = C.current;
      if (d > I.offsetTop - b && d < (S == null ? void 0 : S.offsetTop) - b) {
        o(I);
        return;
      }
    }), d > g.offsetTop - b && o(g);
  }, [r, l, f, e]);
  return (0, import_react.useEffect)(() => {
    const d = f ? e : e == null ? void 0 : e.current;
    return m(), d == null || d.addEventListener("scroll", m), () => {
      d == null || d.removeEventListener("scroll", m);
    };
  }, [m, e, f]), (0, import_jsx_runtime.jsx)("div", { className: a, ...n, children: (0, import_jsx_runtime.jsx)("ul", { className: "nav flex-column nav-pills menu-sidebar", children: (0, import_jsx_runtime.jsx)(ie.Provider, { value: { activeElement: c, setTargets: u }, children: s }) }) });
};
var Tt = ({
  className: e,
  collapsible: t,
  targetRef: s,
  children: r,
  subsections: n,
  onClick: a,
  onActivate: c,
  ...o
}) => {
  var h;
  const { activeElement: l, setTargets: u } = (0, import_react.useContext)(ie), f = () => n == null ? void 0 : n.some((v) => v.current.id === (l == null ? void 0 : l.id)), m = (l == null ? void 0 : l.id) === ((h = s.current) == null ? void 0 : h.id), d = m || f();
  m && (c == null || c(l == null ? void 0 : l.id));
  const b = clsx_m_default("nav-link", t && "collapsible-scrollspy", d && "active", e), g = (v) => {
    const y = s == null ? void 0 : s.current;
    y == null || y.scrollIntoView({ behavior: "smooth" }), a == null || a(v);
  };
  return (0, import_react.useEffect)(() => {
    u((v) => [...v, s]);
  }, [u, s]), (0, import_jsx_runtime.jsx)("li", { className: "nav-item", style: { cursor: "pointer" }, children: (0, import_jsx_runtime.jsx)("a", { className: b, onClick: g, ...o, children: r }) });
};
Tt.defaultProps = {};
var Bt = ({
  collapsible: e,
  className: t,
  children: s,
  style: r,
  ...n
}) => {
  const [a, c] = (0, import_react.useState)("0px"), { activeElement: o } = (0, import_react.useContext)(ie), l = clsx_m_default("nav flex-column", t), u = (0, import_react.useRef)(null);
  (0, import_react.useEffect)(() => {
    const m = () => e == null ? void 0 : e.some((b) => b.current.id === (o == null ? void 0 : o.id)), d = u.current;
    m() ? c(`${d == null ? void 0 : d.scrollHeight}px`) : c("0px");
  }, [o, e]);
  const f = {
    overflow: "hidden",
    height: a,
    transition: "height .5s ease",
    flexWrap: "nowrap",
    ...r
  };
  return (0, import_jsx_runtime.jsx)("ul", { className: l, ref: u, style: e ? f : r, ...n, children: s });
};
Bt.defaultProps = {};
var ps = ({ ...e }) => (0, import_jsx_runtime.jsx)(oe, { type: "checkbox", toggleSwitch: true, ...e });
var Ne = ({ value: e, min: t, max: s, showThumb: r }) => {
  const n = Number(e), [a, c] = (0, import_react.useState)(
    (n || 0 - Number(t)) * 100 / (Number(s) - Number(t))
  ), o = clsx_m_default("thumb", r && "thumb-active");
  return (0, import_react.useEffect)(() => {
    c((Number(e) - Number(t)) * 100 / (Number(s) - Number(t)));
  }, [e, s, t]), (0, import_jsx_runtime.jsx)("span", { className: o, style: { left: `calc(${a}% + (${8 - a * 0.15}px))` }, children: (0, import_jsx_runtime.jsx)("span", { className: "thumb-value", children: e }) });
};
Ne.defaultProps = { min: "0", max: "100" };
var Dt = ({
  className: e,
  defaultValue: t,
  disableTooltip: s,
  labelId: r,
  max: n,
  min: a,
  onMouseDown: c,
  onMouseUp: o,
  onTouchStart: l,
  onTouchEnd: u,
  onChange: f,
  labelClass: m,
  value: d,
  label: b,
  id: g,
  inputRef: h,
  ...v
}) => {
  const [y, $] = (0, import_react.useState)(t), [M, C] = (0, import_react.useState)(false), N = clsx_m_default("form-range", e), S = clsx_m_default("form-label", m);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    b && (0, import_jsx_runtime.jsx)("label", { className: S, id: r, htmlFor: g, children: b }),
    (0, import_jsx_runtime.jsxs)("div", { className: "range", children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "range",
          onMouseDown: (R) => {
            C(true), c && c(R);
          },
          onMouseUp: (R) => {
            C(false), o && o(R);
          },
          onTouchStart: (R) => {
            C(true), l && l(R);
          },
          onTouchEnd: (R) => {
            C(false), u && u(R);
          },
          onChange: (R) => {
            $(R.target.value), f && f(R);
          },
          className: N,
          value: d || y,
          id: g,
          min: a,
          max: n,
          ref: h,
          ...v
        }
      ),
      !s && (0, import_jsx_runtime.jsx)(Ne, { value: d || y, showThumb: M, min: a, max: n })
    ] })
  ] });
};
Dt.defaultProps = { defaultValue: 0 };
var gs = ({ className: e, labelClass: t, labelStyle: s, inputRef: r, size: n, label: a, id: c, ...o }) => {
  const l = clsx_m_default("form-control", `form-control-${n}`, e), u = clsx_m_default("form-label", t);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    a && (0, import_jsx_runtime.jsx)("label", { className: u, style: s, htmlFor: c, children: a }),
    (0, import_jsx_runtime.jsx)("input", { className: l, type: "file", id: c, ref: r, ...o })
  ] });
};
var Mt = import_react.default.forwardRef(
  ({
    className: e,
    children: t,
    noBorder: s,
    textBefore: r,
    textAfter: n,
    noWrap: a,
    tag: c,
    textTag: o,
    textClass: l,
    size: u,
    textProps: f,
    ...m
  }, d) => {
    const b = clsx_m_default("input-group", a && "flex-nowrap", u && `input-group-${u}`, e), g = clsx_m_default("input-group-text", s && "border-0", l), h = (v) => (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: v && Array.isArray(v) ? v.map((y, $) => (0, import_jsx_runtime.jsx)(o, { className: g, ...f, children: y }, $)) : (0, import_jsx_runtime.jsx)(o, { className: g, ...f, children: v }) });
    return (0, import_jsx_runtime.jsxs)(c, { className: b, ref: d, ...m, children: [
      r && h(r),
      t,
      n && h(n)
    ] });
  }
);
Mt.defaultProps = { tag: "div", textTag: "span" };
var Rt = import_react.default.forwardRef(
  ({ className: e, children: t, isValidated: s, onReset: r, onSubmit: n, ...a }, c) => {
    const [o, l] = (0, import_react.useState)(s), u = clsx_m_default("needs-validation", o && "was-validated", e);
    return (0, import_jsx_runtime.jsx)("form", { className: u, onSubmit: (d) => {
      d.preventDefault(), l(true), n && n(d);
    }, onReset: (d) => {
      d.preventDefault(), l(false), r && r(d);
    }, ref: c, ...a, children: t });
  }
);
Rt.defaultProps = {
  noValidate: true
};
var bs = import_react.default.forwardRef(
  ({ className: e, fill: t, pills: s, justify: r, children: n, ...a }, c) => {
    const o = clsx_m_default(
      "nav",
      s ? "nav-pills" : "nav-tabs",
      t && "nav-fill",
      r && "nav-justified",
      e
    );
    return (0, import_jsx_runtime.jsx)("ul", { className: o, ref: c, ...a, children: n });
  }
);
var vs = import_react.default.forwardRef(
  ({ className: e, children: t, style: s, ...r }, n) => {
    const a = clsx_m_default("nav-item", e);
    return (0, import_jsx_runtime.jsx)("li", { className: a, style: { cursor: "pointer", ...s }, role: "presentation", ref: n, ...r, children: t });
  }
);
var hs = import_react.default.forwardRef(
  ({ className: e, color: t, active: s, onShow: r, onHide: n, children: a, ...c }, o) => {
    const l = clsx_m_default("nav-link", s && "active", t && `bg-${t}`, e);
    return (0, import_react.useEffect)(() => {
      s ? r == null || r() : n == null || n();
    }, [s]), (0, import_jsx_runtime.jsx)("a", { className: l, ref: o, ...c, children: a });
  }
);
var $t = import_react.default.forwardRef(
  ({ className: e, tag: t, children: s, ...r }, n) => {
    const a = clsx_m_default("tab-content", e);
    return (0, import_jsx_runtime.jsx)(t, { className: a, ref: n, ...r, children: s });
  }
);
$t.defaultProps = { tag: "div" };
var Et = import_react.default.forwardRef(
  ({ className: e, tag: t, show: s, children: r, ...n }, a) => {
    const [c, o] = (0, import_react.useState)(false), l = clsx_m_default("tab-pane", "fade", c && "show", s && "active", e);
    return (0, import_react.useEffect)(() => {
      let u;
      return s ? u = setTimeout(() => {
        o(true);
      }, 100) : o(false), () => {
        clearTimeout(u);
      };
    }, [s]), (0, import_jsx_runtime.jsx)(t, { className: l, role: "tabpanel", ref: a, ...n, children: r });
  }
);
Et.defaultProps = { tag: "div" };
var ue = (0, import_react.createContext)({
  active: 0
});
var Pt = ({ imagesCount: e, to: t }) => {
  const { active: s } = (0, import_react.useContext)(ue);
  return (0, import_jsx_runtime.jsx)("ol", { className: "carousel-indicators", children: Array.from(Array(e)).map((r, n) => (0, import_jsx_runtime.jsx)("li", { "data-mdb-target": n, className: clsx_m_default(s === n && "active"), onClick: () => t(n) }, n)) });
};
var Ct = ({ move: e }) => (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
  (0, import_jsx_runtime.jsxs)("a", { role: "button", className: "carousel-control-prev", onClick: () => e("prev"), children: [
    (0, import_jsx_runtime.jsx)("span", { className: "carousel-control-prev-icon" }),
    (0, import_jsx_runtime.jsx)("span", { className: "visually-hidden", children: "Previous" })
  ] }),
  (0, import_jsx_runtime.jsxs)("a", { role: "button", className: "carousel-control-next", onClick: () => e("next"), children: [
    (0, import_jsx_runtime.jsx)("span", { className: "carousel-control-next-icon" }),
    (0, import_jsx_runtime.jsx)("span", { className: "visually-hidden", children: "Next" })
  ] })
] });
var It = (e) => {
  const t = getComputedStyle(e), s = getComputedStyle(e == null ? void 0 : e.parentNode);
  return t.display !== "none" && s.display !== "none" && t.visibility !== "hidden";
};
var xt = (e) => Array.from(e == null ? void 0 : e.querySelectorAll(".carousel-item"));
var Lt = (e) => e.offsetHeight;
var kt = (e, t, s = true) => {
  if (!s) {
    me(e);
    return;
  }
  const r = Ot(t);
  t.addEventListener("transitionend", () => me(e), { once: true }), St(t, r);
};
var me = (e) => {
  typeof e == "function" && e();
};
var Ot = (e) => {
  if (!e)
    return 0;
  let { transitionDuration: t, transitionDelay: s } = window.getComputedStyle(e);
  const r = Number.parseFloat(t), n = Number.parseFloat(s);
  return !r && !n ? 0 : (t = t.split(",")[0], s = s.split(",")[0], (Number.parseFloat(t) + Number.parseFloat(s)) * 1e3);
};
var At = (e) => {
  e.dispatchEvent(new Event("transitionend"));
};
var St = (e, t) => {
  let s = false;
  const n = t + 5;
  function a() {
    s = true, e.removeEventListener("transitionend", a);
  }
  e.addEventListener("transitionend", a), setTimeout(() => {
    s || At(e);
  }, n);
};
var Ft = ({
  fade: e,
  className: t,
  dark: s,
  children: r,
  interval: n,
  keyboard: a,
  touch: c,
  showControls: o,
  showIndicators: l,
  onSlide: u,
  ...f
}) => {
  const m = (0, import_react.useRef)([]), d = (0, import_react.useRef)(null), b = (0, import_react.useRef)(0), g = (0, import_react.useRef)(false), [h, v] = (0, import_react.useState)(0), [y, $] = (0, import_react.useState)(0), [M, C] = (0, import_react.useState)({ initialX: 0, initialY: 0 }), [N, S] = (0, import_react.useState)(false), I = (0, import_react.useRef)(null), X = clsx_m_default("carousel", "slide", e && "carousel-fade", s && "carousel-dark", t), A = () => {
    d.current && (clearInterval(d.current), d.current = null);
  }, T = (0, import_react.useCallback)(
    (P, k) => {
      if (k !== void 0)
        b.current = k, v(k);
      else {
        const U = h === y - 1 ? 0 : h + 1, Z = h === 0 ? y - 1 : h - 1;
        b.current = P === "next" ? U : Z, v(P === "next" ? U : Z);
      }
    },
    [h, y]
  ), E = (0, import_react.useCallback)(
    (P, k, U) => {
      var de;
      S(true);
      const z = m.current[h], H = Boolean(d.current), re = P === "next", ne = re ? "carousel-item-start" : "carousel-item-end", ce = re ? "carousel-item-next" : "carousel-item-prev";
      if (k.classList.contains("active")) {
        g.current = false;
        return;
      }
      T(P, U), !(!z || !k) && (g.current = true, H && A(), (de = I.current) != null && de.classList.contains("slide") ? (k.classList.add(ce), Lt(k), z.classList.add(ne), k.classList.add(ne), kt(() => {
        S(false), k.classList.remove(ne, ce), k.classList.add("active"), z.classList.remove("active", ce, ne), g.current = false;
      }, z, true)) : (z.classList.remove("active"), k.classList.add("active"), g.current = false));
    },
    [I, h, T]
  ), R = (P) => {
    g.current || (g.current = true, setTimeout(() => {
      g.current = false;
    }, P));
  }, W = (0, import_react.useCallback)(
    (P) => {
      const k = P === "prev", z = (b.current + (k ? -1 : 1)) % y, H = m.current;
      return z === -1 ? H[y - 1] : H[z];
    },
    [y]
  ), D = (P) => {
    const k = b.current, U = P > k ? "next" : "prev", Z = m.current;
    return { direction: U, nextElement: Z[P] };
  }, F = (P) => {
    if (g.current || (R(700), P > y - 1 || P < 0))
      return;
    const { direction: k, nextElement: U } = D(P);
    E(k, U, P);
  }, O = (0, import_react.useCallback)(
    (P) => {
      if (g.current)
        return;
      R(600);
      const k = W(P);
      E(P, k);
    },
    [W, E]
  ), L = (0, import_react.useCallback)(() => {
    const { visibilityState: P, hidden: k } = document;
    if (P)
      return k || !It(I.current) ? void 0 : O("next");
    O("next");
  }, [I, O]), K = (0, import_react.useCallback)(() => {
    d.current && (clearInterval(d.current), d.current = null), d.current = setInterval(L, n);
  }, [L, n]), Y = (P) => {
    c && C({ initialX: P.touches[0].clientX, initialY: P.touches[0].clientY });
  }, _ = (P) => {
    g.current = true;
    const { initialX: k, initialY: U } = M;
    if (!k || !U)
      return;
    const Z = P.touches[0].clientX, z = P.touches[0].clientY, H = k - Z, re = U - z;
    Math.abs(H) > Math.abs(re) && (H > 0 ? O("prev") : O("next")), C({ initialX: 0, initialY: 0 });
  }, j = () => {
    g.current = false;
  }, q = (0, import_react.useCallback)(
    (P) => {
      switch (P.key) {
        case "ArrowLeft":
          P.preventDefault(), O("prev");
          break;
        case "ArrowRight":
          P.preventDefault(), O("next");
          break;
      }
    },
    [O]
  );
  return (0, import_react.useEffect)(() => {
    if (a)
      return window.addEventListener("keydown", q), () => {
        window.removeEventListener("keydown", q);
      };
  }, [q, a]), (0, import_react.useEffect)(() => {
    const P = I.current, k = xt(P);
    m.current = k, $(k.length);
  }, [I]), (0, import_react.useEffect)(() => {
    u == null || u();
  }, [N, u]), (0, import_react.useEffect)(() => {
    K();
  }, [K]), (0, import_jsx_runtime.jsx)(
    "div",
    {
      onTouchStart: Y,
      onTouchMove: _,
      onTouchEnd: j,
      onMouseEnter: A,
      onMouseLeave: K,
      className: X,
      ref: I,
      ...f,
      children: (0, import_jsx_runtime.jsx)("div", { className: "carousel-inner", children: (0, import_jsx_runtime.jsxs)(ue.Provider, { value: { active: h }, children: [
        l && (0, import_jsx_runtime.jsx)(Pt, { to: F, imagesCount: y }),
        r,
        o && (0, import_jsx_runtime.jsx)(Ct, { move: O })
      ] }) })
    }
  );
};
Ft.defaultProps = { fade: false, interval: 5e3, touch: true, keyboard: false };
var ys = ({
  className: e,
  captionClassName: t,
  children: s,
  src: r,
  alt: n,
  itemId: a,
  video: c,
  ...o
}) => {
  const { active: l } = (0, import_react.useContext)(ue), u = (0, import_react.useRef)(true), f = (0, import_react.useRef)(null), m = clsx_m_default("carousel-caption d-none d-md-block", t);
  return (0, import_react.useEffect)(() => {
    if (u.current && l === a - 1) {
      const d = f.current;
      d == null || d.classList.add("active");
    }
    u.current = false;
  }, [l, a]), (0, import_jsx_runtime.jsxs)("div", { className: "carousel-item", ref: f, children: [
    c ? (0, import_jsx_runtime.jsx)("video", { className: e, autoPlay: true, loop: true, muted: true, ...o, children: (0, import_jsx_runtime.jsx)("source", { src: r, type: "video/mp4" }) }) : (0, import_jsx_runtime.jsx)("img", { className: e, src: r, alt: n, ...o }),
    (0, import_jsx_runtime.jsx)("div", { className: m, children: s })
  ] });
};
var we = import_react.default.createContext({
  activeItem: 0,
  setActiveItem: null,
  alwaysOpen: false,
  initialActive: 0
});
var Wt = import_react.default.forwardRef(
  ({ alwaysOpen: e, borderless: t, className: s, flush: r, initialActive: n, tag: a, children: c, onChange: o, ...l }, u) => {
    const f = clsx_m_default("accordion", r && "accordion-flush", t && "accordion-borderless", s), [m, d] = (0, import_react.useState)(n);
    return (0, import_react.useEffect)(() => {
      m && o && o(m);
    }, [o, m]), (0, import_jsx_runtime.jsx)(a, { className: f, ref: u, ...l, children: (0, import_jsx_runtime.jsx)(we.Provider, { value: { activeItem: m, setActiveItem: d, alwaysOpen: e, initialActive: n }, children: c }) });
  }
);
Wt.defaultProps = { tag: "div", initialActive: 0 };
var Xt = import_react.default.forwardRef(
  ({
    className: e,
    bodyClassName: t,
    bodyStyle: s,
    headerClassName: r,
    collapseId: n,
    headerTitle: a,
    headerStyle: c,
    btnClassName: o,
    tag: l,
    children: u,
    ...f
  }, m) => {
    const { activeItem: d, setActiveItem: b, alwaysOpen: g, initialActive: h } = (0, import_react.useContext)(we), [v, y] = (0, import_react.useState)(h), $ = clsx_m_default("accordion-item", e), M = clsx_m_default("accordion-header", r), C = clsx_m_default("accordion-body", t), N = clsx_m_default(
      "accordion-button",
      g ? n !== v && "collapsed" : n !== d && "collapsed",
      o
    ), S = (I) => {
      g ? y(I !== v ? I : 0) : b(I !== d ? I : 0);
    };
    return (0, import_jsx_runtime.jsxs)(l, { className: $, ref: m, ...f, children: [
      (0, import_jsx_runtime.jsx)("h2", { className: M, style: c, children: (0, import_jsx_runtime.jsx)("button", { onClick: () => S(n), className: N, type: "button", children: a }) }),
      (0, import_jsx_runtime.jsx)(
        he,
        {
          id: n.toString(),
          show: g ? v === n : d === n,
          children: (0, import_jsx_runtime.jsx)("div", { className: C, style: s, children: u })
        }
      )
    ] });
  }
);
Xt.defaultProps = { tag: "div" };
var _t = ({
  className: e,
  size: t,
  contrast: s,
  value: r,
  defaultValue: n,
  id: a,
  labelClass: c,
  wrapperClass: o,
  wrapperStyle: l,
  wrapperTag: u,
  label: f,
  onChange: m,
  children: d,
  labelRef: b,
  labelStyle: g,
  inputRef: h,
  onBlur: v,
  readonly: y,
  ...$
}) => {
  var Y;
  const M = (0, import_react.useRef)(null), C = (0, import_react.useRef)(null), N = b || M, S = h || C, [I, X] = (0, import_react.useState)(r || n), [A, T] = (0, import_react.useState)(0), [E, R] = (0, import_react.useState)(
    r !== void 0 && r.length > 0 || (n !== void 0 && n.length) > 0
  ), W = clsx_m_default("form-outline", s && "form-white", o), D = clsx_m_default("form-control", E && "active", t && `form-control-${t}`, e), F = clsx_m_default("form-label", c);
  (0, import_react.useEffect)(() => {
    var _;
    N.current && ((_ = N.current) == null ? void 0 : _.clientWidth) !== 0 && T(N.current.clientWidth * 0.8 + 8);
  }, [N, (Y = N.current) == null ? void 0 : Y.clientWidth]);
  const O = () => {
    N.current && T(N.current.clientWidth * 0.8 + 8);
  };
  (0, import_react.useEffect)(() => {
    r !== void 0 && (r.length > 0 ? R(true) : R(false));
  }, [r]), (0, import_react.useEffect)(() => {
    n !== void 0 && (n.length > 0 ? R(true) : R(false));
  }, [n]);
  const L = (_) => {
    X(_.currentTarget.value), m && m(_);
  }, K = (0, import_react.useCallback)(
    (_) => {
      I !== void 0 && I.length > 0 || r !== void 0 && r.length > 0 ? R(true) : R(false), v && v(_);
    },
    [I, r, v]
  );
  return (0, import_jsx_runtime.jsxs)(u, { className: W, style: { ...l }, children: [
    (0, import_jsx_runtime.jsx)(
      "textarea",
      {
        readOnly: y,
        className: D,
        onBlur: K,
        onChange: L,
        onFocus: O,
        defaultValue: n,
        value: r,
        id: a,
        ref: S,
        ...$
      }
    ),
    f && (0, import_jsx_runtime.jsx)("label", { className: F, style: g, htmlFor: a, ref: N, children: f }),
    (0, import_jsx_runtime.jsxs)("div", { className: "form-notch", children: [
      (0, import_jsx_runtime.jsx)("div", { className: "form-notch-leading" }),
      (0, import_jsx_runtime.jsx)("div", { className: "form-notch-middle", style: { width: A } }),
      (0, import_jsx_runtime.jsx)("div", { className: "form-notch-trailing" })
    ] }),
    d
  ] });
};
_t.defaultProps = { wrapperTag: "div", readonly: false };
var jt = ({
  children: e,
  invalid: t,
  feedback: s,
  tooltip: r,
  tag: n,
  ...a
}) => {
  const [c, o] = (0, import_react.useState)(null), l = (0, import_react.useRef)(null), u = clsx_m_default(
    t ? `invalid-${r ? "tooltip" : "feedback"}` : `valid-${r ? "tooltip" : "feedback"}`
  );
  return (0, import_react.useEffect)(() => {
    var m, d;
    const f = (d = (m = l.current) == null ? void 0 : m.querySelector("input, textarea")) == null ? void 0 : d.parentElement;
    f && o(f);
  }, []), (0, import_jsx_runtime.jsxs)(n, { ref: l, ...a, children: [
    c && (0, import_react_dom.createPortal)((0, import_jsx_runtime.jsx)("div", { className: u, children: s }), c),
    e
  ] });
};
jt.defaultProps = {
  tag: "div",
  feedback: "Looks good!"
};
export {
  Wt as MDBAccordion,
  Xt as MDBAccordionItem,
  Pe as MDBBadge,
  Ht as MDBBreadcrumb,
  Ue as MDBBreadcrumbItem,
  ae as MDBBtn,
  xe as MDBBtnGroup,
  ke as MDBCard,
  We as MDBCardBody,
  Xe as MDBCardFooter,
  je as MDBCardGroup,
  Oe as MDBCardHeader,
  Qt as MDBCardImage,
  Zt as MDBCardLink,
  _e as MDBCardOverlay,
  Ae as MDBCardSubTitle,
  Fe as MDBCardText,
  Se as MDBCardTitle,
  Ft as MDBCarousel,
  ys as MDBCarouselItem,
  os as MDBCheckbox,
  Ee as MDBCol,
  he as MDBCollapse,
  $e as MDBContainer,
  ls as MDBDropdown,
  is as MDBDropdownItem,
  us as MDBDropdownMenu,
  ds as MDBDropdownToggle,
  gs as MDBFile,
  es as MDBFooter,
  zt as MDBIcon,
  nt as MDBInput,
  Mt as MDBInputGroup,
  Ye as MDBListGroup,
  Ve as MDBListGroupItem,
  fs as MDBModal,
  Nt as MDBModalBody,
  vt as MDBModalContent,
  bt as MDBModalDialog,
  wt as MDBModalFooter,
  ht as MDBModalHeader,
  yt as MDBModalTitle,
  Qe as MDBNavbar,
  ze as MDBNavbarBrand,
  He as MDBNavbarItem,
  Ze as MDBNavbarLink,
  et as MDBNavbarNav,
  tt as MDBNavbarToggler,
  ts as MDBPagination,
  ss as MDBPaginationItem,
  st as MDBPaginationLink,
  mt as MDBPopover,
  pt as MDBPopoverBody,
  gt as MDBPopoverHeader,
  rt as MDBProgress,
  ve as MDBProgressBar,
  cs as MDBRadio,
  Dt as MDBRange,
  be as MDBRipple,
  Ge as MDBRow,
  ms as MDBScrollspy,
  Tt as MDBScrollspyLink,
  Bt as MDBScrollspySubList,
  Le as MDBSpinner,
  ps as MDBSwitch,
  rs as MDBTable,
  as as MDBTableBody,
  ns as MDBTableHead,
  bs as MDBTabs,
  $t as MDBTabsContent,
  vs as MDBTabsItem,
  hs as MDBTabsLink,
  Et as MDBTabsPane,
  _t as MDBTextArea,
  qe as MDBTooltip,
  Ke as MDBTypography,
  Rt as MDBValidation,
  jt as MDBValidationItem
};
//# sourceMappingURL=mdb-react-ui-kit.js.map
